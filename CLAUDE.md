# 단어 시험 (베트남어 ↔ 한국어) — 인수인계서

> 이 문서는 새 작업 세션(Claude Code / claude.ai/code)이 이 프로젝트를 바로 이어서 작업할 수 있도록 정리한 핸드오버 문서입니다.

## 1. 프로젝트 개요
- GYBM 베트남 과정 참가자(한국인, 베트남어 학습자)를 위한 **베트남어 ↔ 한국어 단어/문장 시험·암기 웹앱**.
- **`tiengviet.html` 파일 하나로 동작**하는 자기완결 앱: 프레임워크·빌드·서버 불필요, 더블클릭하면 브라우저에서 열림, **오프라인 작동**, 데이터는 브라우저 **localStorage**에 저장.
- 순수 **바닐라 JS**(외부 라이브러리 없음). UI는 한국어.

## 2. 파일 구조
- **`tiengviet.html`** — 앱 본체(HTML+CSS+JS 한 파일). 거의 모든 작업은 여기서.
- `index.html` — 루트 URL용 리디렉트(→ `tiengviet.html`). GitHub Pages 주소를 깔끔하게 하려고 둠. 수정 거의 안 함.
- `README.md`, `.gitignore`, 이 `CLAUDE.md`.
- `contents.zip` / 이미지 — 교재 사진 원본. **.gitignore로 제외**(저장소에 안 올라감).

## 3. 실행 / 미리보기 (중요)
- 이 환경엔 **node가 없음**. JS 검증은 브라우저 미리보기로 함.
- 미리보기 서버: `prj_code/.claude/launch.json`의 **`wordnote`** 설정(python http.server, 포트 8137, `word_note` 폴더 serve). `preview_start name=wordnote`.
- 파일명은 `tiengviet.html`(2026-07-14에 `단어시험.html`에서 변경 — 앱 내용이 단어시험을 넘어서서). URL 인코딩 불필요.
- 검증은 `preview_eval`로 함수 직접 호출(예: `WORDS.length`, `start()`, `loadNote()`) + `preview_screenshot`.
- **⚠️ preview 서버가 샌드박스 권한오류로 못 뜰 때(2026-06-27 지속)**: 미리보기 프로세스가 `word_note`(심지어 `prj_code`)를 못 읽어 404. 그땐 **인쇄/PDF 레이아웃은 Chrome 헤드리스로 검증** 가능(Bash는 샌드박스 아님): ①`tiengviet.html`을 scratchpad에 복사하고 끝에 `<script>setTimeout(()=>{P.onePage=true;P.withKey=true;genPaper(true);},400)</script>` 주입 ②`"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless=new --no-pdf-header-footer --print-to-pdf=out.pdf --virtual-time-budget=3000 file://.../test.html` ③PDF 페이지수 확인+Swift(PDFKit)로 페이지를 PNG 렌더해 Read로 눈으로 확인(`mdls -name kMDItemNumberOfPages`도 가능). 시험지 페이지수·압축 맞춤은 이 방식으로 실측했음.
  - **앱 화면 스샷**(인쇄 아닌 일반 화면 검증): 같은 복사본 끝에 `<script>setTimeout(()=>{actions.goList()/*등 액션 호출*/},400)</script>` 주입 후 `chrome --headless=new --screenshot=out.png --window-size=430,1500 --force-device-scale-factor=2 file://...` → PNG를 Read. 아이콘 교체는 이 방식으로 7화면 검증함.

## 4. 데이터 구조
```js
const RAW = { 1:`베트남어|한국어뜻\n...`, 2:`...`, ... 12:`...` };  // 단원별, 줄마다 "베트남어|뜻"
const SENTENCES = `베트남어 문장|한국어뜻\n...`;                      // 예문(문장)
```
- **⚠️ 중요**: 코드에서 뜻(meaning) 필드 이름은 **`en`** 이지만 **실제로는 한국어**가 들어있음(원래 영어였다가 한국어로 교체한 흔적). `vn`=베트남어, `en`=한국어 뜻.
- 단어 **532개**(Lesson 1~12 = 496개 + **13단원 "오늘 배운 ⭐" 36개**, 사용자가 그날 배운 단어 모음). 단원 추가는 RAW에 같은 형식. RAW 키는 숫자여야 함(`buildWords`가 `Number(L)`). 13단원 라벨은 `LESSON_NAMES`로 "오늘 배운 ⭐" 표시.
- **문장 데이터 2종(별개!)**: ① `SENTENCES`/`SENTS` — 단원 구분 없는 53개, **실전시험(`startExam`)**용. ② `SENT_RAW`={단원번호:문장} + `SENT_BY_LESSON`/`sentLessons()` — **✍️ 문장 시험·시험지 문장범위**용, 현재 **1과 53·2과 62·3과 56·4과 81·5과 63·6과 88·7과 75 = 478개(L1~7)**. 교재 사진→문장 추출(+쓰기연습 정답 생성분), 한국어는 직접 번역. (섹션5 참고)
  - **⚠️ 시험지 "전체"=`allSentences()`(=②의 단원 합집합 122개), `SENTS`(①53개) 아님**(2026-06-27 수정). 예전엔 "전체"가 옛 SENTS 53개라 단원 문장이 하나도 안 나오는 버그였음(사용자 발견). `allSentences = () => sentLessons().flatMap(l=>SENT_BY_LESSON[l])` 헬퍼로 단원 추가 시 자동 반영. 실전시험은 여전히 SENTS 사용.
- 사용자 진도: 단어 L1~12 + 그날 학습 단어(13단원)+7/2복습(14단원), 문장은 **1~6단원** 넣음. 새 단원 문장은 교재 사진 받아 `SENT_RAW`에 번호 키로 추가.

## 5. 주요 기능 & 코드 위치 (함수명)
- 데이터 빌드: `buildWords()` — `WORDS` 배열(각 항목 `{id,lesson,vn,en,num,gi}`) 생성. 단어 추가(localStorage)도 합침.
- 문제 생성: `makeQ(word,dir,type)`, 방향 `dir`은 `"vn2en"`(베→한) / `"en2vn"`(한→베) / 랜덤.
- 홈/영역: `renderHome()`=**읽기·듣기·쓰기·말하기 4영역 카드**(`SKILLS`,`SKILL_ORDER`)+보조버튼(실전·플래시·단어장…). 카드 클릭→`goSkill(skill)`→`renderSetup()`(범위·문제수 선택)→`startSkill()`. `state.skill`(LS `_skill`)에 마지막 영역 저장. **방향은 모든 영역(실전 제외)에서 베→한/한→베/랜덤 선택**(2026-06-25, `showDir=skill!=="exam"`; `goSkill` 진입 시 영역 기본 `dir` 세팅, `startSkill`은 `exam`만 random 고정). 말하기는 카드별 `SP.cards[].dir`(`renderSpeak`가 방향 따라 베트남어/한국어 먼저 보여줌).
- 시험 진행: `startSkill()`(영역 시작·표준 경로) / `start()`(구버전, 현재 홈에선 미사용), `startExam()`(실전 단어40+문장10), `startNote()`(오답노트), `setupQ()`, `renderQuiz()`, `next()`.
- ✍️ 문장 시험(단원별, 2026-06-24): `SENT_RAW`={단원번호:`베트남어 문장\|한국어`}(현재 L1~7, 총 478개 — 단원별 수는 섹션4/9 참고) + `SENT_BY_LESSON`/`sentLessons()`. 홈 "✍️ 문장 시험"→`goSent`→`renderSentSetup`(단원·방향 선택)→`startSent`(`sentence` 타입 자가채점). **기존 `SENTENCES`/`SENTS`(실전시험·시험지용, 단원 구분 없는 53개)와는 별개**. 단원 추가는 `SENT_RAW`에 번호 키로 문장 넣으면 됨(교재 사진→문장 추출, 한국어 뜻은 직접 번역).
- 듣기(`mode:"listen"`) = **베트남어 받아쓰기**(2026-07-10 개편): 소리(vi-VN)만 듣고 **들은 베트남어를 그대로 입력**. 설정(`renderSetup`)에서 **단어/문장** 선택(`state.studyKind` word|sent, LS `_skind` — 읽기·쓰기·듣기·말하기 공용), **방향 선택 없음**(받아쓰기 고정). 문제객체 `{dir:"listen", dictation:true, prompt=answer=vn, meaning=en}`(`listenWordQ`/`listenSentQ`, 문장은 `startSentQueue("listen")`가 `SENT_BY_LESSON`에서). `renderQuiz`의 `isListen` 분기 — 글자 숨기고 🔊 자동재생(`.bigspk`/`.listenbox`, `!answered&&!revealed`일 때만), 정답 공개 시 베트남어+**뜻(한국어)** 표시. 단어 채점=`checkVnTyped`(성조까지, **그 단어만**·동의어 인정 X → `Q.toneOnly`로 "성조만 틀림" 안내), 문장 채점=`gradeSent`(성조까지 3단계 ok/tone/no; 성조만 틀리면 경고+자가채점). 오답 재시도는 `rebuildQ`로 받아쓰기 복원(`retryWrong/retryLast`, dictation이면 `state.mode="listen"`). (옛 "베트남어 듣고 한국어 뜻 입력" 모드는 대체됨.)
- **여러 문제: 순서 선택 + 이어서 풀기**(2026-07-14): 쓰기+문장에 **문제 순서** 세그 추가 — `state.sentOrder`(LS `_sord`, 기본 random): `book`=교재 순서 / `random`=섞기. `startSentQueue`가 `keepOrder`면 `shuffle` 안 함.
  - **여러 문제(bulk)는 이제 그 단원 문장 전부**를 10개씩 나눠 품(문제 수 카드 숨김). `startSentBulk(arr, lbl, lesson, resumeFrom)`.
  - **진행 기억**(교재 순서일 때만): LS **`_bulkpos`** = `{단원: 다음에 풀 페이지}`. `bulkNext`가 페이지 넘길 때 저장, **끝까지 다 풀면 지움**(다음엔 처음부터). 랜덤은 순서가 매번 달라 저장 안 함. 헬퍼 `bulkPosGet/bulkPosSet/bulkPosAll`.
  - 설정에 **`Lesson 3 — 21번부터 이어서 풀어요` 배너 + `[처음부터]`**(`bulkReset`), 시작 버튼도 `21번부터 이어서 풀기`로 바뀜. 단원별로 따로 기억됨.
- **단어 순서 배치**(`mode:"order"`, 2026-07-12): 쓰기 → 문장 → **푸는 방식 3택**(`state.sentMode`, LS `_sbulk`에 문자열로 저장 — 옛 boolean 값은 부팅 시 `one`/`bulk`로 변환): `one`(한 문제씩) / `bulk`(여러 문제) / `order`(순서 배치). `startSentQueue`가 `startSentOrder`로 분기 → 상태 `ORD` + `ordSetup`/`renderOrder`/`ordCheck`/`ordNext`/`renderOrderDone`. **한국어 뜻을 보여주고 흩어진 베트남어 낱말 칩을 눌러 순서대로 배열**(방향 고정이라 방향 카드 숨김). 낱말 = 공백 단위(`ordWords`, 구두점은 붙은 채로 — 교재 표기 유지). 문장은 **낱말 3~14개**만 출제(너무 짧/긴 것 제외, 남는 게 3개 미만이면 전부 사용). 섞을 때 원래 순서면 다시 섞음. 채점은 **낱말 조각이 그대로라 순서만 맞으면 정답**(문자열 join 비교). 오답 → 정답+내 답 비교 표시, 오답노트(sentence)로. 액션 `sentMode/ordPick/ordUnpick/ordClear/ordShuffle/ordCheck/ordNext/quitOrder`. CSS `.wline/.wpool/.wchip{.placed,.used}`.
- **문장 빈칸 채우기**(듣기, 2026-07-12): 듣기 → **문장** → '어떻게 풀까요?' **[빈칸 채우기] [받아쓰기]**(`state.listenSent`, LS `_lsent`, 기본 cloze). cloze면 **대화 빈칸 화면·채점을 그대로 재사용** — `startSentCloze(arr, lesson, lbl)`가 문장 하나를 대화의 '한 줄'처럼 만들어(`who`="1." "2." …) `DLG.lines`에 넣고 `DLG.kind="sent"`로 표시. 빈칸 생성(`dlgBuildLine`)·채점(`dlgGrade`, 성조까지)·틀린 것만 다시(`dlgRetryWrong`)·힌트·전체 듣기 전부 공용. 난이도는 `state.dlgLevel` 공용. `다음 →`은 `nextSentCloze`(그 단원에서 문장 새로 뽑기), `처음부터`는 같은 문장에 빈칸만 새로. 화면 키 `dlg:{kind}:{단원}:{batch|대화}:{retry}`.
- **받아쓰기 코너 (교재 Chính tả)**(듣기, 2026-07-14): 홈 → 듣기 → **[대화] [문장] [받아쓰기]** 3택(`state.studyKind==="ct"`, 듣기 전용). **교재의 받아쓰기 지문만 따로 집중 연습**(사용자 요청 — 그 전엔 문장 풀에 섞여만 있었음). `startCt()`가 **대화 '전부 받아쓰기' 화면·채점을 그대로 재사용**(`DLG.kind="ct"`, `DLG.full=true`) — 화자가 없는 글이라 `who`는 번호("1." "2." …). 설정은 **단원 칩만**(범위·문제수·난이도 없음) + 한국어 뜻 보기/숨기기. 헤더 `Lesson 7 · 받아쓰기 · 119쪽`, 듣기 버튼 `지문 듣기`, `[다음 대화]`는 숨김(지문이 하나뿐). `state.ctLesson`(LS `_ctL`).
  - **데이터 표시법**: `SENT_RAW`에 **`#133 ct`** 처럼 쪽 마커 뒤에 `ct`를 붙이면 그 구간이 받아쓰기 지문(`s.ct=true`). 지문이 끝나면 **`#133`(ct 없이)로 되돌려** 같은 쪽의 쓰기 문제와 구분. 쪽만으론 구분 불가(8과 133쪽엔 받아쓰기 8 + 쓰기 원문/생성정답 16이 섞여 있음). 헬퍼 `ctSents(l)` / `ctLessons()`. **마찬가지로 `#쪽 rd`=읽기 지문(ĐỌC HIỂU, `s.rd=true`, `rdSents`/`rdLessons`) — 파서 정규식은 `(ct|rd)?`.**
  - **7개 단원 57문장**: 2과 29쪽 6 · 3과 47쪽 10 · 4과 65쪽 8 · 5과 79쪽 7 · 7과 119쪽 11 · 8과 133쪽 8 · 9과 147쪽 7. (1·6과는 교재에 Chính tả 없음)
  - ⚠️ 새 단원 추가 시 받아쓰기 지문 앞에 `#쪽 ct` 마커, 끝나면 `#쪽` 마커를 꼭 넣을 것.
- **대화 전부 받아쓰기**(듣기, 2026-07-14): 듣기 → **대화** → '어떻게 풀까요?' **[빈칸 채우기] [전부 받아쓰기]**(`state.listenDlg`, LS `_ldlg`, 기본 cloze). full이면 `startDlg`가 **빈칸 없이**(`parts:[]`,`blanks:[]`) 줄만 만들고 `DLG.full=true`, `DLG.nb=줄 수`. `renderDlg`가 full이면 **줄마다 `textarea.dfull` 하나**(화자·한국어 뜻만 보이고 베트남어는 숨김, 줄별 🔊). 채점은 `dlgGrade`의 full 분기 — **줄마다 `gradeSent`**(성조까지 ok/tone/no, 문장 받아쓰기와 같은 기준). 통계 단위가 '빈칸'→'줄'로 바뀜(`unit`). **'틀린 것만 다시'는 full에서 숨김**(줄 단위라 잠금 개념이 안 맞음) → 대신 `다시 받아쓰기`. Enter=다음 줄, Shift+Enter=줄바꿈. 난이도 카드는 빈칸 모드에서만 보임. **한국어 뜻 보기/숨기기 토글**(`state.dlgKo`, LS `_ldlgko`, 기본 보임) — 문제 화면 위 세그로 전환, 숨기면 줄마다 `[뜻 보기]`(`DLG.hint[li]` 재사용)로 그 줄만 볼 수 있음. 채점 후엔 뜻 항상 보이고 토글은 숨김. 토글 시 `dlgSaveInputs()`로 **쓰던 내용 유지**.
- **대화 빈칸 채우기**(`mode:"dlg"`, 2026-07-12): 듣기 → 설정에서 **대화** 선택. 데이터 `DIALOG_RAW`(`화자|베트남어|한국어`, 1~7과 대화 20개/139줄)→`DIALOGS`/`dialogLessons()`. `startDlg`→`renderDlg`(상태 `DLG`). 빈칸=`dlgBuildLine`(그 단원 새 단어 우선 `dlgVocab` tier, 난이도로 개수, 매번 랜덤), 듣기=`speakLines`(줄 순서 재생+하이라이트), 힌트=줄별 한국어 뜻 토글, 채점=`dlgGrade`(`checkVnTyped` 성조까지 ok/tone/no, 오답 단어는 오답노트로). **`SENT_RAW`(문장)와 별개 데이터** — 회화 원문이 온전히 들어있음. (섹션9 참고)
- 말하기(`mode:"speak"`): `startSpeak/renderSpeak/speakAdvance/renderSpeakDone`(상태 `SP`). 🔊 듣고 따라 말한 뒤 자가채점(✅ 잘했어요 / 🔁 더 연습=오답노트 추가). 점수 없음.
- **남부(호치민)말 → 하노이말 병기**(2026-07-17): 교재가 호치민에서 만들어져 **남부말만 실린 단어**가 있음. `NORTH = {"trễ":"muộn","mắc":"đắt"}` + `northOf(vn)` / `northTag(vn)`(회색 `.ntag` 배지) / `NORTH_N`(하노이말→교재말, 검색용). **단어장(`renderList`)·홈 검색(`bindFind`) 표시 + 쓰기 정답 인정(`checkVnAny`가 `northOf(expected)`를 후보에 추가)**에만 씀 — **⚠️ 교재 뜻(`en`)·`SENT_RAW` 문장은 절대 안 건드림**(시험은 교재 기준). 검색은 `muộn`/`đắt`로 쳐도 `trễ`/`mắc`가 나옴(`northHit` + `rank` 0순위).
  - **교재도 9·10과부턴 스스로 병기함**: `muộn/trễ`·`ba mẹ/bố mẹ`·`anh/anh trai`·`chị/chị gái` 등 슬래시 표기(`acceptable`이 슬래시를 복수정답으로 처리). 그래서 `trễ`는 **6과(단독) + 9과(`muộn/trễ`)** 둘 다 있음 — 중복 아니라 교재 그대로. **NORTH에 추가할 땐 교재에 이미 슬래시로 있는지 먼저 확인**(있으면 불필요).
  - **어순(하노이 `không + V + được` ↔ 호치민 `V + O + không được`)은 데이터를 안 고침** — 교재가 두 어순을 **반반 섞어 씀**(남부 3곳: 8과 122·123쪽·대화1 / 북부 3곳: 3과 34쪽, 8과 133·134쪽. 8과는 문법설명=북부인데 회화=남부). **채점이 이미 양쪽 다 정답**(`gradeSent`의 "정답 낱말이 다 들어있으면 ok" 규칙 — 낱말이 같고 순서만 다르므로) → 손댈 필요 없음.
- 채점: `norm()`, `acceptable()`, `isCorrectTyped()` — 대소문자·`to`/관사·괄호·슬래시·콤마 복수정답 관대 처리. **답에 든 숫자만 입력해도 정답**(예: "숫자 15, 열다섯"→"15"). **⚠️ 베트남어 답(`en2vn` 단어)은 성조·đ까지 채점**(`checkVnTyped`/`normTone`/`acceptableTone`, 2026-06-25): 성조까지 맞아야 정답, 철자만 맞고 성조 틀리면 `Q.toneOnly`→"⚠️ 성조가 틀려요(철자는 맞아요)" 안내(성조 학습 위해). **문장은 자가채점**(reveal→맞음/틀림).
  - **⚠️ `gradeSent`의 "tone"(성조 차이) 판정 = 성조를 떼면 똑같을 때만**(2026-07-17 버그수정). 예전엔 `sentMatch`(편집거리 80% 유사)로 판정해서 **`mắc`(비싸다)↔`rẻ`(싸다)처럼 뜻이 정반대인 한 글자 차이도 '성조 차이'**로 나왔음(오답이어야 함). 지금은 `normSent`(성조 제거) 후 **완전 일치 또는 낱말집합 일치**여야 tone, 아니면 no. `sentMatch`는 그대로 남아 `checkSent`의 관대 자동정답에만 쓰임.
- 시험지(인쇄): `genPaper()`, `renderPaper()`, state `P`(format: `wordsent`=단어40+문장10 / `words`=단어만). 중복 단어 자동 제거. `window.print()`. @media print로 컨트롤 숨김.
  - **우선출제**(`P.priority` 토글, 시험지 화면의 "📌 오늘 배운 ⭐ 단어 먼저 출제"): 켜면 '오늘 배운 단어'(`isReviewWord` = 13단원 ∪ `REVIEW_EXTRA` 기존단어 norm매칭)를 선택 범위에 없어도 포함하고 시험지 앞쪽에 배치. 새 시험 단어 묶음을 우선시킬 땐 13단원에 추가하거나 `REVIEW_EXTRA`에 vn 추가.
  - **문장 범위**(`P.sentPick`=Set 다중선택, 2026-07-09, wordsent 2부 문장): "전체"(빈 Set → `allPaperSentences()` = 모든 단원 문장 합 478개(L1~7)) 또는 단원 여러 개 토글선택(`sentLessons().filter(has).flatMap(paperSents)`). `paperSentLesson` 액션(토글), 시험지 화면 "문장 범위" 칩. `genPaper`에서 `sentPool` 분기. (컨트롤 클릭 시 스크롤 유지=`paperKeep`.)
  - **정답지에 문장 출처 + 교재 쪽번호 표시**(2026-07-14): 시험지 2부 문장의 **정답지에만** `Lesson 5 회화2 · 70쪽` / `Lesson 8 · 134쪽` 을 회색 작은 글씨로(`.psrc`). **시험지 본지엔 안 나옴**(실제 시험 때 힌트 방지, 사용자 요청).
    - **쪽 데이터 넣는 법**: `SENT_RAW`에 **`#122` 같은 마커 줄**을 넣으면 그 아래 문장들에 `page`가 붙음(다음 마커까지 유지). `#129-130`처럼 범위도 됨. 문장 672개를 일일이 안 달고 **섹션 덩어리 단위**로 처리 — 문장이 교재 순서대로 저장돼 있어서 가능. 파서(`SENT_BY_LESSON` 빌드)가 마커 줄을 걸러냄.
    - **회화 쪽**은 `DIALOG_PAGES = {1:[3,4,4], 2:[17,17,18], …}`(대화별 쪽) → `DIALOGS[l][i].page`. `sentSource(vn, lesson, page)`가 **문장을 `DIALOGS`와 대조**해 회화면 대화 번호+쪽을, 아니면 마커 쪽을 반환. `genPaper`가 `P.sentQs[].src`에 담아둠.
    - **1~8과 672문장 전부 쪽번호 있음(100%)**. 사진에서 섹션별 쪽 확인해 마커 삽입: 1과 9개 · 2과 10 · 3과 8 · 4과 12 · 5과 11 · 6과 12 · 7과 11 · 8과 17.
    - ⚠️ 새 단원 추가 시 `SENT_RAW`에 `#쪽` 마커도 같이 넣고 `DIALOG_PAGES`에 대화 쪽 추가할 것.
  - **정답지(시험지와 동일 레이아웃) + 시험지/정답지 따로 저장**: `renderPaper`가 시험지 `.paper` 뒤에 `.paper.pkey` 정답지 블록을 **항상** 추가. **정답지는 시험지와 똑같은 `.pq`(단어)·`.psq`(문장) 구조**를 쓰되 빈 밑줄(`.pqline`/`.psline`) 자리에 **정답을 초록으로 채움**(`.pqline.answ`/`.psline.answ`, 2026-06-27 개편 — 옛 `.pkeygrid/.pkq`의 `프롬프트 → 정답` 한 줄 형식 폐기). 그래서 번호·열정렬·압축이 시험지와 100% 동일. **인쇄 버튼 2개**(`printPaperOnly`/`printKeyOnly` → `printOnly("paper"|"key")`): `body`에 `ponly-paper`/`ponly-key` 클래스를 잠깐 붙여 `@media print`에서 한쪽만 보이게 한 뒤 `window.print()`, `afterprint`+setTimeout 백업으로 제거 → **시험지 PDF / 정답지 PDF 따로 저장**. `ponly-key`일 땐 `.pkey` `break-before` 무효화로 빈 첫 장 방지. (옛 `P.withKey` 토글은 대체·제거됨.) 정답지 압축 미세조정: `.paper.onepage .pqline.answ`/`.psline.answ`로 1장 유지. ⚠️ 문장 정답은 `.psq` 기본 폰트를 물려받아 너무 컸던 걸 `.psline.answ{font-size:13px}`(onepage 11px)로 본문보다 작게 지정 → 밑줄 안에 들어옴(단어 정답은 `.pq` 폰트 상속이라 OK).
  - **A4 한 장에 맞춤**(`P.onePage` 토글 "📄 A4 한 장에 맞춤", **기본 ON**, 2026-06-27): 켜면 두 `.paper`에 `onepage` 클래스 → `.paper.onepage *` 압축 CSS(폰트·줄간격·여백 축소)로 단어40+문장10이 **A4 1장**에 들어감(시험지·정답지 각각 1장). `paperOne` 액션(`renderPaper()`만). 압축값은 실제 인쇄 PDF로 맞춤(문장 10번째까지 1장 보장).
  - **단어 번호 세로(열 방향) 정렬**(2026-06-27): 2열 단어 그리드를 행→지그재그 대신 **왼쪽 열 1~N 내려간 뒤 오른쪽 열**로. `.pgrid`/`.pkeygrid.colflow`에 `grid-auto-flow:column; grid-template-rows:repeat(var(--prows),auto)`, 인라인 `style="--prows:${Math.ceil(wQs.length/2)}"`로 행 수 지정(2열 미디어=인쇄·≥560px에만 적용, 폰 1열은 그대로 순차). 시험지·정답지 번호 순서 일치.
- 오답 노트: `loadNote/saveNote/noteWrong/noteRight/noteAdd`(키=`lc(vn)||lc(en)`), `renderNote()`. 오답 자동수집, 별표(`starWord`) 직접추가, 연속 `GRAD(=2)`번 정답 시 졸업(제거). `renderNote`에 **시험 방향**(베→한/한→베/랜덤, `noteDir`→`state.dir`) 선택. `startNote`는 듣기/말하기 모드 진입 시 주관식(`type`)으로 폴백. **오답 직접 추가**(2026-06-25): `renderNote` 상단 폼(베트남어+뜻 입력) → `addNoteWord`/`addNoteSent` 액션 → `addNoteManual("word"|"sentence")` → `noteAdd`(lesson="내 오답", `noteHas` 중복체크). 누구나 종이 시험 오답을 앱에서 직접 입력(동기 공유 대비).
  - **시험 오답 미리 등록** (⚠️ **2026-06-25 비활성화**: 동기 공유 위해 `seedNote()`를 빈 함수로 만듦 → 새 사용자는 빈 오답노트, 기존 등록분은 각 기기 localStorage 유지, `SEED_NOTE` 배열은 코드에 잔존하나 미사용. 되살리려면 `seedNote` 본문 복원): `SEED_NOTE`(배열, `{vn,en,lesson,type?}`) + `seedNote()` — 앱 첫 실행 시 1회 오답노트에 머지(`_seedv`=`SEED_NOTE_V` 플래그). 같은 단어가 이미 있으면 `lesson`·`type`만 갱신(streak 유지), 없으면 추가. **새 시험 오답 추가/라벨 정정 시 `SEED_NOTE` 수정 후 `SEED_NOTE_V` 값을 바꾸면** 다음 로드 때 반영됨(기존 등록분도 갱신). 단어는 `type` 생략(word), 작문은 `type:"sentence"`(자가채점). `lesson`엔 시험 라벨(문자열 "6/22 시험" 등 — `lessonLabel`이 문자열은 그대로 표시).
- 백업/복원: `exportData()`, `importBackup()`(합치기/중복제외), `renderBackup()`.
- Telex 도움말: `renderTelex()`.
- 기타: `renderHome()`, `renderList()`(단어장), `renderAdd()`(단어추가), `renderFlash()`(플래시카드), `selectionBlockHTML()`(단원/범위 공용), `speak()`(🔊 Web Speech: 베=vi-VN, 한=ko-KR).
- **⚠️ 폰에서 소리 안 남 → 고침**(2026-07-14, 동기들 제보 "되다가 갑자기 안 돼"). **원인 2개 다 내가 만든 것**:
  - ① **keep-alive가 음성을 '멈춤'으로 굳힘(안드로이드 주범)**: 2026-07-11에 *긴 문장 중간 정지*를 막으려 **8초마다 `pause()`+`resume()`**을 돌렸는데, `resume()`이 실패하면 **speechSynthesis가 paused로 굳어 이후 speak()이 전부 조용히 쌓이기만 함** → 새로고침 전까지 무음. `ttsKeepStop()`이 `resume()`을 안 불러서 한번 굳으면 영영 안 풀렸음. **게다가 이 '중간 정지'는 PC 크롬만의 버그** — 폰엔 없는 병에 약을 쓴 셈. → **`IS_MOBILE`이면 keep-alive 아예 안 돌림**(`ttsKeepStart`), `ttsKeepStop`/`stopSpeak`/`speak` 모두 **`paused`면 `resume()`으로 풀어줌**(자가 복구).
  - ② **`setTimeout(80)`이 아이폰 user gesture 끊음**: 아이폰 사파리는 **탭한 그 순간**에 `speak()`이 호출돼야 소리가 남. → **이미 말하는 중일 때만 80ms 지연**, 아니면 즉시 재생.
  - ⚠️ **앞으로 speak를 setTimeout/Promise 안에서 부르지 말 것**(아이폰 무음). **폰에서 pause/resume 하지 말 것**(굳음).
- **⚠️ 카톡 링크로 바로 열면 소리 안 남 → 감지·안내**(2026-07-14, 사용자 제보 "카톡에서 바로 링크를 통해 열면 발음이 안 들려"). **인앱 브라우저(카톡·인스타 등)는 Web Speech를 지원 안 함** — 특히 **안드로이드 카톡은 Chrome WebView라 `getVoices()`가 아예 빈 배열** → 코드를 아무리 고쳐도 소리 안 남. **앱 잘못이 아니라 카톡 브라우저의 한계.** → `IN_APP`(UA에 `KAKAOTALK|Instagram|FBAN|FBAV|FB_IAB|NAVER(inapp|Line/|DaumApps|; wv)`)로 감지 → `ttsProblem()`이 `"inapp"` 반환 → **홈·발음설정에 노란 배너 + `[크롬 · 사파리로 열기]`**(`openExternal`). `openExternal`은 **카톡이면 `kakaotalk://web/openExternal?url=` 스킴**(안드·아이폰 둘 다 됨), 그 외 안드로이드면 `intent://...package=com.android.chrome`, 나머지는 alert 안내. ⚠️ **동기에게 공유할 땐 "카톡에서 바로 열지 말고 브라우저로 열어주세요"라고 알려주거나, 홈화면에 추가해서 쓰라고 안내할 것.**
- **음성 없을 때 안내**(2026-07-14): `ttsProblem()` — 브라우저 미지원 / **기기에 vi 음성 없음**(`VOICES`는 있는데 `viVoices()`가 0)이면 안내 문자열 반환. 홈에 노란 배너 + `[해결 방법 보기]`(→`goVoice`). 한국 폰은 베트남어 TTS가 기본 설치 안 된 경우가 많음.
- **발음(소리) 설정**(2026-07-10): `renderVoice`/`goVoice`(홈 하단 '발음(소리) 설정' 버튼). 앱은 자체 발음엔진 없음 — **기기 내장 TTS**(Web Speech)를 씀. 기기 vi 음성 목록(`viVoices`=`VOICES` 중 lang이 vi)에서 **음성 선택**(LS `_voiceURI`, 미선택 시 enhanced/premium/neural/natural 이름 우선=`pickedVoice`)+**속도 조절**(LS `_rate`, 슬라이더 0.6~1.3배=`ttsRate`, 기본 0.9). `speak`가 **베트남어일 때만** 선택 음성 적용(한국어 등은 OS 기본). `loadVoices`+`onvoiceschanged`로 목록 로드. 액션 `pickVoice`(선택 저장+미리듣기)·`testVoice`(샘플 "Tôi có thể nói tiếng Việt." 재생). 하노이/호치민 억양은 기기 음성이 결정(대개 북부 표준).
- **교재 문법어 35개 추가**(2026-07-14): `RAW`(단어장)엔 **교재 부록4 단어 목록**만 있어서, 교재가 **문법·발음 섹션에서 가르치는 낱말**이 통째로 빠져 있었음(`được` 41회·`sẽ` 34회·`quá`·`đang`·`hả`·`đừng`·`sắp`… 문장엔 나오는데 검색·단어장·대화빈칸 tier0에 안 잡힘). 사용자 요청으로 **교재 정의 그대로** 뜻을 달아 각 단원에 추가 — 2과 3(`nó`·`dạ`·`ơi`) · 3과 2(`bốn`·`mười`) · 4과 5(`đang`·`thế nào`·`cho`·`cần`·`tiền`) · 5과 6(`sẽ`·`quá`·`đấy`·`à`·`sao`·`khách sạn`) · 6과 2(`chỗ`·`hoặc`) · 7과 4(`sắp`·`hả`·`nên`·`vì`) · 8과 13(`được`·`đừng`·`mới`·`giùm`·`ngoại ngữ`·`thứ tiếng`·`nghe nói`·`hình như`·`thảo luận`·`khóc`·`đọc`·`xin`·`nói được`). **WORDS 574→609.** 효과: 8과 대화 빈칸의 '그 단원 새 단어' 비율 6/13→11/18. ⚠️ 단원별 단어 수가 늘어 **시험지 구성이 조금 달라짐**(사용자 동의).
- **내 단어에 영어 뜻(선택)**(2026-07-14): `renderAdd`에 **영어 뜻 칸(`#inEng`)** 추가 — 넣으면 `_custom`에 `{vn,en,eng}`로 저장되고 `buildWords`가 `w.eng`로 실어줌 → **교재 단어와 똑같이** 홈 검색(영어)·시험지 `en2vn` 프롬프트·Word 저장·단어목록에 쓰임. **영어는 선택**(없으면 `{vn,en}` 그대로, 한국어만 필수). ⚠️ 백업(`importBackup`)·클라우드(`mergeCloud`)가 `{vn,en}`만 복사해 **eng를 버리던 것도 같이 고침**(중복키는 여전히 `vn||en`).
- **⚠️ 단어 추가 '자동완성' 제거**(2026-07-17, 사용자 요청): `lookupLocal`·`translateOnline`·`koDictForm`·`fillEngFromBook`·`doAutoFill`·`addBusy`·`autoFill` 액션·`.needcheck`/`.addstatus.warn`/`.busy` CSS **전부 삭제**. **사용자가 베트남어·뜻을 직접 기입**(영어는 선택). 이유: 번역기가 `ghi nhớ → 기억하기`(사전형 아님)처럼 어설픈 답을 채워 넣으면 **지우고 고치는 게 처음부터 직접 쓰는 것보다 번거로움**. **이제 단어 추가에서 인터넷을 아예 안 씀**(fetch 0회 — jsdom으로 검증). Enter = 베·한 둘 다 있으면 추가, 아니면 빈 칸으로 포커스. `setAddStatus`는 '추가됐어요'/'지웠어요'에 계속 씀(`ok`만). 되살리려면 git 이력 참고.
- **시험 대비(간격 반복 SRS)**(2026-07-18): 홈 상단 파란 CTA `시험 대비 · D-n`(`goPrep`)→`renderPrep`. **8/1 1200단어 시험 대비**로 만든 스프린트 모드. 단어마다 **숙련도 0~3**(`SRS_MAX=3`, 3=마스터), 저장 `_srs`={`단원|normTone(vn)`:레벨} — **각자 폰 로컬(개인 진도, cloudSave 안 함)**, 단어 자체는 코드라 동기 공유. `srsKey/srsAll/srsLv/srsRec`, `prepCounts()`(master/familiar/fresh/total), `prepDaysLeft()`·`prepDlabel()`(`PREP_EXAM=2026-08-01`, 기기 날짜로 매일 자동 D-day). 세션(`prepStart`): **약한 단어 우선**(`srsLv` 오름차순 → 하위 20개 → 세션 내 셔플), **한국어 뜻 보고 베트남어 성조까지 쓰기**(`checkVnAny`) — 맞으면 `srsRec(w,true)`(+1), 틀리면(오답·모름) 0으로 리셋, **성조만 틀리면(tone) 진행·리셋 둘 다 안 함**+경고. `renderPrepQuiz`(진행바·숙련도 점·프롬프트=`w.en`·입력·채점/모르겠어요, 채점 후 정답 공개+🔊+`northTag`)→`prepGrade`/`prepSkip`/`prepNext`→`renderPrepDone`(정답률·마스터 집계). state `PREP`, 화면키 `prep`/`prepq:N`/`prepdone`. deck=전체 WORDS(교재+내단어). **기존 '실전 시험'(exam)과 다른 기능** — 이름 충돌 피해 `prep`. jsdom 검증(D-14·+1/리셋/tone·약한우선·마스터 3캡·집계·회귀).
- **문법 정리**(2026-07-17): 홈 '문법 정리' 버튼(`goGrammar`)→`renderGrammar`. **각 단원 문법 포인트를 한국어 설명으로**(사용자 요청). 데이터 `GRAMMAR = {단원:{name, items:[{t:제목, d:설명}]}}`(1~9과) — **예문 블록 없이 패턴+짧은 설명만**(사용자가 '설명만 간단히' 택함). `gramLessons()`=단원 목록. 화면: 단원 칩(`gramLesson` 액션, 마지막 본 단원 LS `_gramL`)+`Lesson N · 단원명` 헤딩+`.gcard`(`.gt` 제목/`.gd` 설명). `state.screen="grammar:N"`(단원 바꾸면 맨 위로 — 새 단원이라 자연스러움). **⚠️ SENT_RAW·RAW 데이터는 안 건드림** — 순수 참고용 화면. 내용은 교재가 가르치는 포인트를 교재 예문으로 확인해 작성(1·2·3·6과=사진 예문 확인, 4·5·7·8·9과=핵심 particle). 단원 추가/수정은 `GRAMMAR`에 번호 키만.
- **홈 단어 검색 → 그 단어가 나오는 교재 문장**(2026-07-14): `bindFind`가 단어 목록 아래에 **그 낱말이 든 교재 문장·대화 줄**을 최대 25개 보여줌(`findSentences`/`sentHasTerm`/`markTerms`). 검색은 **베트남어 · 한국어 뜻 · 교재 영어 뜻(`w.eng`)** 다 됨. ⚠️ **오탐 방지 2단**: ①`sentHasTerm`은 **부분일치가 아니라 낱말 단위 일치**(`đi`가 `điện` 안에 안 걸림), ②찾는 순서를 **좁은 것부터** — `입력 그대로+성조까지` → 없으면 `검색된 단어의 베트남어+성조까지` → 없으면 `성조 무시`. 이렇게 안 하면 `đừng`이 `đúng`·이름 `Dung`을, `mới`가 `mời`를 끌어옴(실제로 발생해서 고침). 찾은 낱말은 문장 안에서 굵게(`markTerms`). ⚠️ **검색 매칭은 '낱말이 시작하는 자리'에서만**(`startsHit`) — 부분일치로 하면 `đi`(성조 떼면 `di`)가 `chăm chỉ`의 영어뜻 **diligent** 안에 걸리고, `hả`(`ha`)가 **chăm** 안에 걸림(둘 다 사용자가 신고). 한 낱말 검색은 `토큰.startsWith(q)`(→ `đi`가 `đi học`○ / `chăm`✗, `ngo`→`ngoại ngữ`○), 여러 낱말 검색(`ngân hàng`)은 `t.startsWith(q)||t.includes(" "+q)`. 영어 뜻(`engHit`)은 **낱말 통째 일치**(bank→bank), 4글자 이상일 때만 앞부분 허용(teach→teacher). 한국어 뜻은 교착어라 낱말 앞부분 일치로 충분(`운전`→`운전하다`). ⚠️ **단어 목록은 8개까지만**(`WMAX`) 보여주고 `rank()`로 **성조까지 일치 → 뜻/영어 일치 → 성조 무시 일치 → 시작 → 포함** 순 정렬 — 안 그러면 `đi` 검색에 `điện`·`đường`이 37개 먼저 떠서 **아래의 교재 문장이 파묻혀 안 보임**(사용자가 '그거 어디서 봐?' 하고 물어본 실제 사례). 성조를 안 보면 `đừng` 검색에 `đúng`이 첫 줄에 떴음.
- **스크롤 유지**(2026-07-12): 예전 `set()`은 그릴 때마다 `scrollTo(0,0)` → 칩·토글·힌트 하나만 눌러도 맨 위로 튐(사용자 지적). 이제 `set()`이 **`state.screen` 키를 이전 렌더와 비교** → **같은 화면이면 스크롤 자리 유지**, **다른 화면이면 맨 위로**. 화면 키에 **인덱스를 붙여** 진짜 이동일 때만 위로 감: `setup:{skill}` · `quiz:{Q.idx}` · `flash:{F.idx}` · `speak:{SP.idx}` · `dlg:{단원}:{대화}:{retry}` · `bulk:{page}` · `list` · `add` · `note` · `paper` · `home` · `result` 등. **새 화면을 만들면 `state.screen`을 반드시 세팅할 것**(안 하면 이전 키가 남아 스크롤이 안 올라감). `refresh()`는 `startsWith("setup")`로 비교. (기존 `paperKeep`은 그대로 둠 — 이제 중복이지만 무해.)
- 클릭 처리: 하단 `actions` 맵 + `view.addEventListener("click", ...)` (data-action 위임).

## 6. 자주 하는 변경
- **단어 추가**: `RAW`의 해당 단원 백틱 문자열에 `베트남어|한국어뜻` 줄 추가.
- **문장 추가(단원별)**: `SENT_RAW`에 단원 번호 키로 `베트남어 문장|한국어뜻` 줄. (`SENTENCES`는 실전·시험지용 기존 53개라 거의 안 건드림)
- **방향**: 홈엔 방향 버튼 없음(영역 카드). 방향은 `renderSetup`의 방향 세그(`setupDir`)·오답노트(`noteDir`)·문장시험(`sentDir`)에서. 영역별 기본은 `SKILLS[].dir`. `DIRLABEL`, `promptLang`/`ansLang`(vi-VN/ko-KR).

## 7. 컨벤션 & 주의사항
- **아이콘(이모지 금지, 2026-06-27)**: UI 그림 이모지는 **인라인 SVG 라인 아이콘**으로 교체함. `<body>` 첫머리 **`<svg><defs><symbol id="i-*">` 스프라이트**(home/book/note/pencil/cards/volume/plus/check/x/printer/star/eye/eye-off/headphones/mic/save/keyboard/alert/repeat/bulb/shuffle/pin/target/inbox/clipboard/folder/search/trophy/thumb) + CSS `.ic{width:1em;height:1em;stroke:currentColor;fill:none…}`. 사용: `<svg class='ic' viewBox='0 0 24 24'><use href='#i-NAME'/></svg>`(크기는 부모 font-size=1em). **새 UI에도 이모지 대신 이 아이콘 쓸 것**(없으면 `<symbol>` 추가). ⚠️ **속성값(placeholder/title/value)·`<option>`·`alert()`엔 SVG 못 넣음 → 거기선 문자**. **유지(문자)**: 방향 화살표 `→ ← ↔`, 언어 국기 `🇰🇷 🇻🇳`, 별표 토글 `☆ ★`(빈/채움 구분 위해). 이모지 컬러별 `⭐→★`.
- **색상**: 저채도 차분한 팔레트(`:root` CSS변수 `--brand:#5f6fa6` 등). 쨍한 원색 금지(사용자 요청).
- **인쇄 시험지**: 종이에는 **이모지·안내문 없음**(미니멀). `@page{margin:0}`로 브라우저 머리글/바닥글 제거. (시험지/정답지는 2026-06-27부터 **"시험지 저장"·"정답지 저장" 버튼으로 따로 PDF 출력** — `printOnly`. 미리보기엔 둘 다 표시.)
- **표기**: 교재 따라 "**cám ơn**"(O), "cảm ơn"(X).
- **localStorage 키**(접두사 `gybm_wordnote_v1`): `_notebook _custom _mode _dir _count _selMode _skill _skind _sbulk _sord _bulkpos _lsent _ldlg _ldlgko _ctL _dlgL _dlgN _dlvl _gramL _passL _srs _pcw _pcs _seedv _wrong _zoom _voiceURI _rate _lastbk`. 브라우저/기기마다 따로 저장 → **백업/복원** 필요(홈 맨 아래 작은 회색 링크로 축소함, 사용자 요청). ⚠️ **KEY(`gybm_wordnote_v1`)·도메인·`_custom`/`_notebook` 구조를 바꾸거나 `localStorage.clear()`를 넣으면 사용자 데이터가 날아감 → 절대 금지**(코드 push 자체로는 안 지워짐). (`_skind`=단어/문장, `_voiceURI`/`_rate`=발음, `_lastbk`=백업 넛지 기준 카운트.) (`_zoom`=화면 확대 배율 0.8~2.0, 헤더 −/100%/+ 버튼.)
- **단원 선택(`state.lessons`)은 LS 저장 안 함**: 앱 시작 시 1회만 전체선택(시작부 `lessonsList().forEach(add)`), 이후 '전체 해제'가 유지됨(`renderSetup`/`goHome`은 자동 전체선택 안 함 — 예전엔 여기서 자동선택해 '전체 해제'가 무효화되던 버그였음). 시험지(`genPaper`)는 범위 0개여도 alert로 튕기지 않고 `renderPaper`에서 "범위 골라주세요" 안내(`1f36d1f`, ⚠️ preview 권한오류로 미검증).
- 홈은 **4영역(읽기·듣기·쓰기·말하기)** 중심. **4영역 모두 양방향**(베↔한, `renderSetup`에서 방향 선택). 홈 카드 방향 태그는 읽기·쓰기 **둘 다 "베 ↔ 한"으로 통일**(사용자 요청). 읽기/쓰기는 둘 다 주관식(type)이라 사실상 방향만 다른 비슷한 기능. 실전시험·플래시카드는 하단 보조. 객관식 모드 제거됨(죽은 코드 잔존).

## 8. GitHub / 배포
- 원격: **`chan026-normal/word-note`** (Public — GitHub Pages 때문에).
- Pages URL(폰에서 사용): **https://chan026-normal.github.io/word-note/**
- 커밋 이메일은 **noreply**로 설정됨(이 repo git config user.email = `chan026-normal@users.noreply.github.com`, local). 실제 이메일 노출 금지.
- 워크플로: **작업 전 `git pull`**(사용자가 폰 claude.ai/code로 먼저 고쳤을 수 있음), 작업 후 `commit + push`. **커밋에 `Co-Authored-By:` 줄은 넣지 않음**(사용자 요청 2026-07-10 — 개인 학습앱이라 불필요). 커밋 메시지는 변경 내용만 간결히.
- 사용자는 비개발자 → 기술은 쉬운 비유로 설명. 한국어로 소통.

## 9. 현재 상태 & 미해결 (2026-07-19 세션)

### ▶ 다음 세션이 먼저 볼 것 (인수인계 요약, 2026-07-19 기준)

- **⚠️ 이 저장소에 아직 없는 작업**: 아래 2026-07-19 Cowork 항목의 **① 맨 위로 가기 버튼 ② 지문 해석 섹션 바로가기**는 **Cowork 샌드박스 사본에만 있고 이 저장소엔 반영 안 됨**(확인: `0c6a923`에 `#toTop`·`updToTop`·`passageJump`·`.pgjump` 모두 **0개**). 그 기능이 들어간 `tiengviet.html`을 받아야 커밋 가능. (이 줄은 원래 "커밋만 하면 됨"으로 잘못 적혀 있었음 — 2026-07-21 정정.)
- **⚠️ 이 저장소는 세션이 둘 이상 동시에 손댐**: **Cowork(샌드박스)** 와 **맥의 Claude Code**. 실제로 이번에 겹쳤음 — Cowork가 `직접 출제`·`회색선 제거`를 만드는 사이 Claude Code가 같은 걸 커밋(`927c7e2`,`56b4c43`,`952f967`)해서, Cowork 쪽 미커밋분은 `git stash` 후 버려지고 Claude Code 버전이 정본이 됨. **작업 시작 전 반드시 `git log --oneline -5` + `git status`로 최신 상태 확인**, 큰 작업은 중간중간 커밋할 것.
- **⚠️ 검증 환경**: Cowork 샌드박스엔 **브라우저가 없음** → `jsdom`으로만 검증(부팅 오류·데이터·화면 렌더·클릭 전환). **인쇄(A4 시험지) 레이아웃과 실제 폰 화면은 확인 불가** → 사용자 확인 필요. (예전엔 맥 Chrome 헤드리스로 PDF 실측했음 — 섹션3 참고.) **2026-07-21 추가**: 맥 Claude Code 쪽도 **헤드리스 크롬이 멈춰서 못 씀**(정적 파일조차 2분 타임아웃) → 임시폴더에 `npm i jsdom` 해서 부팅 검증하는 게 확실함(`runScripts:"dangerously"` + `beforeParse`로 `speechSynthesis`/`matchMedia`/`IntersectionObserver` shim, `jsdomError` 수집). ⚠️ `WORDS`는 최상위 `let`이라 `window.WORDS`로 안 잡힘 → 렌더된 `#view` 텍스트("0 / 1226 마스터")로 확인할 것.
- **현재 데이터(2026-07-21, `0c6a923`)**: WORDS **1226**(교재 1~12과 531 + 개인세트 13·14 78 + Expats 15~26 270 + **흔한 초급 어휘 27~32과 347**) · 문장 **819**(L1~9) · 대화 **26**(L1~9) · 읽기 지문 `rd` 8개 단원 · 받아쓰기 `ct` 7개 단원. (27필수동사105·28필수형용사79·29음식45·30집물건옷56·31사람몸동물31·32자연장소생활31)
- **8/1 1200단어 시험 대비**가 현재 최우선 맥락 — 홈 최상단 `시험 대비` 배너(`goPrep`), 간격반복 `_srs`. **1226으로 1200 목표는 넘김**(`0c6a923`).

- **2026-07-19 (Cowork — 맨 위로 버튼 + 지문 해석 섹션 바로가기)** ⚠️ **이 저장소엔 미반영 — 샌드박스 사본 전용 설계 기록**: 사용자 요청 2건.
  - **① 맨 위로 가기**: `<body>` 끝(`.foot` 뒤)에 `#toTop` 버튼(`.totop`, `noprint`) + `#i-chev`를 `rotate(-90deg)`로 위쪽 화살표. `updToTop()`이 `window.scrollY>260`일 때만 `.show`, `scroll` 리스너 + **`set()` 끝에서도 호출**(화면 바뀌면 상태 갱신). `scrollToTop()`은 `behavior:"smooth"`(구형 대비 try/catch). **모든 화면 공용**.
  - **② 지문 해석 바로가기**: `renderPassage`의 `sec()`에 **id 인자 추가** → `#pgsec-dia|rd|ct`. 그 단원에 **있는 섹션만** 칩으로(`.pgjump`, `passageJump` 액션), **2개 이상일 때만** 표시. `scrollToEl()`이 **붙어있는 topbar에 안 가리게 −64px 보정**.
  - jsdom: 부팅0 · 버튼 표시/숨김·클릭 스크롤 · 5과 3개 칩/1과 2개 칩 · 섹션 id · 회귀(홈·WORDS 879·직접 출제 `custParse`/`mixCustom` 동작).

- **2026-07-19 (Claude Code — 홈 화면 다듬기)**: ① 홈 목록('연습 더'·'복습·관리') **회색선 제거** — `.lcard` border 삭제 + `.lrow` border-bottom 삭제 **+ `.lrow` `border:none`**, 선 대신 여백(`.lrow` padding 13→17px, `.lcard` padding 2→8px; 카드 배경·그림자 유지). ⚠️ `.lrow`는 `<button>`이라 `background:none`만으론 **브라우저 기본 테두리**가 남아 폰(삼성 인터넷)에서 줄로 보였음 → 앱의 다른 background:none 버튼(`.link`/`.h-resume`/`.h-banner`)처럼 **`border:none` 명시 필수**(56b4c43에서 빠뜨려 후속 커밋으로 수정). ② **시험 대비 배너(`h-banner`)를 홈 최상단으로** 이동(hero 카드 위, 8/1 시험 집중). 순수 표시 변경 — 데이터·로직 무관.

- **2026-07-19 (Cowork — 시험지 '직접 출제' 신설)**: 사용자 요청 "동기생이 시험지를 커스텀 제작 / 문장을 직접 타이핑 / **응시자가 직접 낸 문제와 자동 배치 문제를 구별**하게". 시험지 설정에 **직접 출제 입력칸 2개**(`#pcw` 단어 · `#pcs` 문장, LS `_pcw`/`_pcs`) — 한 줄에 **`베트남어|한국어뜻`**(구분자 `|` 하나만 — 콜론은 베트남어 문장의 시간표기 `8:30` 등과 충돌해 **사용자 지시로 `|` 전용**, `:`는 안 씀). `custParse`/`custQs`로 파싱(뜻 없으면 베→뜻 방향 고정·정답칸 공백, `custom:true`). ⚠️ 안내문·placeholder에 **em dash(—) 쓰지 말 것**(사용자 요청).
  - **genPaper**: 직접 출제 문항은 **반드시 포함**. 겹치는 자동문항은 **`lc(vn)` 기준으로 제외**(중복 방지) → 같은 단어가 두 번 안 나옴. 자동문항 순서(우선출제 ★ 정렬)는 **그대로 두고 직접문항만 `mixCustom`으로 무작위 위치에 삽입** → 총 개수(단어 40 / 문장 10) 유지 + 직접문항은 섞이되 **★ 앞정렬은 안 깨짐**. 사용자가 고른 방식은 **"섞되 표시만"**(별도 섹션 아님). ⚠️ 직접문항이 상한(40/10)을 넘으면 전부 포함하므로 총수가 넘을 수 있음(안내문 '40'과 불일치 가능하나 점수 만점 `${total}`은 동적이라 맞음).
  - **구별 표시**: 문항 번호 옆에 **연필 아이콘(`.pcust`, `#i-pencil`)** — **시험지·정답지 양쪽** 모두. 직접 문항이 하나라도 있을 때만 머리말 아래 작은 범례 `✎ 직접 출제한 문제`(`.plegend`). 이모지 대신 SVG(섹션7 규칙).
  - **입력칸 바인딩**(`bindPaperCustom`): 타이핑 중(`input`)엔 **저장만**(다시 그리면 커서가 튀므로), 칸을 벗어날 때(`change`)만 `paperKeep(()=>genPaper(false))`로 시험지 재생성.
  - ⚠️ **인쇄 레이아웃은 실측 못 함**(샌드박스에 브라우저 없음) — 범례 한 줄이 늘어 'A4 한 장 맞춤'이 빠듯해질 수 있음. 사용자 인쇄 확인 필요(어긋나면 `.plegend` 여백/`onepage` 값 조정).
  - jsdom: 부팅0·파싱·직접문항 전부 포함(단어3/문장2)·총수 40/10 유지·위치 분산(섞임)·시험지+정답지 표시 12개·범례 유무·저장값 복원·회귀(WORDS 879·홈).
  - **2026-07-19 (Claude Code — 커밋 전 적대적 리뷰 후 3건 수정)**: ① 구분자 `:`→**`|` 전용**(콜론이 베트남어 본문과 충돌해 오파싱되던 것). ② 바깥 `shuffle([...cw,...qs])`가 **우선출제 ★ 앞정렬을 깨던 회귀** → `mixCustom(auto,cust)`(자동 순서 유지+직접문항만 무작위 삽입)로 교체. ③ **직접↔자동 중복**(범위에 이미 있는 단어를 직접 출제하면 두 번 나옴) → `cwVn=Set(lc(vn))`로 자동문항 사전 제외. node 단위검증(콜론안전·`|`분리·순서보존·중복제거) 통과. 인쇄 레이아웃은 여전히 사용자 확인 필요.

- **2026-07-19 (Cowork — 지문 해석 화면 신설: 대화·읽기·받아쓰기)**: 사용자 요청 "각 단원 dialogue를 해석만 한 부분, Reading·dictation도". 시험·빈칸 없이 **읽기용 참고 화면**. 홈 '지문 해석' 버튼(`goPassage`)→`renderPassage`. 단원 칩(`passageLesson`, LS `_passL`) → **대화(`DIALOGS`)·읽기(`rdSents`)·받아쓰기(`ctSents`)** 지문을 베트남어+한국어 나란히, 줄마다 🔊(`spkBtn`). 화면키 `passage:N`.
  - **읽기 지문 태그 신설**: SENT_RAW 파서에 **`#쪽 rd` 마커**(ct처럼) 추가 → `s.rd=true`, 헬퍼 `rdSents(l)`/`rdLessons()`. 읽기(ĐỌC HIỂU) 지문은 문장 풀에 섞여 있어 따로 표시가 없던 걸, **연결된 지문 블록을 확인 후 그 쪽 마커에 `rd`를 붙임**(데이터 내용 안 바꾸고 플래그만). **8개 단원**: 1과 p8(5) · 3과 p47(7) · 4과 p57(9) · 5과 p78(6) · 6과 p94(11) · 7과 p114(10) · 8과 p128(11) · 9과 p142(8). (2과는 연결된 지문이 아니라 개별 문장 모음이라 제외.) ⚠️ **rd와 ct는 안 겹침**(같은 47쪽이라도 읽기=`#47 rd`, 받아쓰기=`#47 ct` 따로). 읽기 지문은 여전히 읽기·쓰기·순서배치 모드에도 그대로 나옴(rd는 추가 플래그일 뿐).
  - jsdom: 부팅0·읽기 8단원/rd·ct 안겹침·문장 819 유지·화면(대화·읽기·받아쓰기 3섹션·한국어·5과 첫문장)·단원전환·`_passL` 저장·회귀(홈·문법·검색병기·받아쓰기 7단원·WORDS 879).

- **2026-07-19 (Cowork — 《Useful Vietnamese for Expats》 어휘 추가: Section 1 완료)**: 사용자가 PDF 2권(《Useful Vietnamese for Expats》 1·2권, 스캔 이미지)을 올림. **⚠️ 이 책은 앱의 원교재(1~12과 «CHÀO ANH» 등)와 별개 자료** — 사용자 지적 "다른 자료인데 '2권'이라 하면 같은 시리즈로 오해". 그래서 라벨은 **"Expats · 주제"**(권/과 번호 없이, `LESSON_NAMES` 15~26). 8/1 1200단어 시험 대비 어휘 보강용. 책 1권은 앱 원교재와 주제가 겹쳐 대부분 이미 있음, **책 2권이 빠진 절반**(분류사·생활용품·오토바이·위치·집·가구·일상·날짜·아픔·날씨·취미·여행)이라 그걸 `RAW` 15~26과로 추가(13·14는 개인 세트라 안 건드림). **책2 전체(토픽 1~12) = 15~26과 완료, +270단어 → WORDS 879.**
  - 15과 35 · 16과 24 · 17과 19 · 18과 22 · 19과 17 · 20과 15 (Section 1) · 21과 33(하루일과·시간·빈도) · 22과 28(요일·날짜표현) · 23과 24(신체·통증·증상) · 24과 18(날씨) · 25과 15(취미) · 26과 20(여행·지방). 중복(앱에 이미 있는 흔한 낱말 몇 개)은 그대로 둠 — 무해.
  - 추출법: 스캔본이라 OCR(베트남어 팩 없음) 불가 → **페이지 이미지를 직접 눈으로 읽어 성조까지 전사**(pdftoppm으로 outputs/pv2에 렌더 후 Read). 각 토픽 'I./II. VOCABULARY' 그림목록에서 베트남어 추출, 영어(=뜻)를 **한국어로 번역**해 `vn|한국어뜻`. ENG_RAW엔 아직 안 넣음(한국어 뜻만 — 시험대비는 한→베라 충분).
  - **⚠️ 고유명사·지명(Bản Giốc, Hạ Long 등)·순수 문법입자는 제외**, 내용어(명사·동사·형용사) 위주. 새 단원 추가 시 `RAW`에 번호 키, `LESSON_NAMES`에 "Expats · 주제".
  - **⚠️ 번호 표기 혼동 방지**(사용자 지적 "원래 교재는 12과까지인데 13과부터 시작하면 교재 연장선으로 오해"): 13·14(개인)·15~26(Expats)은 **내부 번호일 뿐 화면엔 이름으로** 표시(오늘 배운 ★ · 7/2 복습 · Expats · 분류사…). 대부분 이미 `lessonLabel` 썼지만 **시험지 범위(`rangeTitle`)·단어장 라벨(`curListLabel`)·Word 저장(`lessonTag`)이 맨숫자 "15과"로 새고 있던 걸 수정** → 새 헬퍼 `lessonName(l)`(이름 있으면 이름, 없으면 "N과")로 통일. **원래 교재 1~12과는 "1과~12과" 그대로.**
  - jsdom: 부팅0·15~26과 개수·형식오류0·시험대비 덱 포함(879)·홈검색(냉장고→tủ lạnh·산→núi)·`lessonName`(1→1과·15~26→Expats, rangeTitle에 15과 안뜸)·회귀. 문장 819·대화 26 유지.

- **2026-07-18 (Cowork — 시험 대비 SRS 모드 신설)**: 동기 단톡방에 **8/1(토) 부원장님 주도 1200단어 시험**(빈칸에 아는 단어 다 적기, 3시간 30분) 공지 → "학습 연구자 입장에서 최대 효율" 요청. 학습과학 3원칙(분산·인출연습·약한단어 집중) 설명 후 **간격 반복(라이트너) '시험 대비' 모드** 구현. `PREP`/`_srs`(숙련도 0~3, 약한 단어 우선, 한→베 성조 채점). D-day 카운트다운(기기 날짜 자동). **진도는 개인(로컬), 단어는 코드(공유)** 구조 사용자에게 설명함. (섹션5 시험 대비 참고)
  - **⚠️ 단어 범위 미해결**: 앱 WORDS=609(교재 1~12과 부록 505 + 문법낱말 + 13/14과 개인세트)인데 **시험은 1200개**. 조사 결과 **본문 단어까지 합쳐도 ~550~600**(교재가 부록 단어를 본문에서 재사용) → 1200은 **앱에 없는 단원/더 긴 부록**에서 옴. **부원장님 1200단어 목록이나 교재 전체 부록을 구하면 코드에 넣어 동기 전원 공유**할 것. 지금은 609로 시작(시스템은 단어 수 무관하게 굴러감).
  - jsdom: 부팅0·D-14·정답+1/오답0/성조tone·약한단어우선·마스터3캡·집계·`_srs`로컬·회귀(홈·문법·검색병기·단어장·단어추가). WORDS 609·문장 819 유지.

## (이전) 현재 상태 & 미해결 (2026-07-17 세션)

- **2026-07-17 (Cowork — 문법 정리 화면 신설)**: 사용자 요청 "각 단원의 문법 파트를 따로 정리해놓은 부분". 물어봐서 **내용=제가 한국어로 정리 / 형식=설명만 간단히(예문 없이)** 택함. `GRAMMAR` 데이터(1~9과, 단원당 3~5개 포인트, 각 `{t:제목, d:설명}`) + `renderGrammar`/`goGrammar` + 홈 '문법 정리' 버튼(오답노트 아래). 단원 칩으로 전환, 마지막 본 단원 `_gramL` 저장. **패턴·핵심어 위주, 전체 예문 문장은 안 넣음.** 데이터는 안 건드림(순수 참고 화면). 내용은 SENT_RAW 예문으로 각 단원 문법 확인 후 작성(1과 có...không/không·호칭, 2과 phải không·của·cũng, 3과 ai/gì·đã·với, 4과 đang·rất/lắm·bao nhiêu, 5과 sẽ·chúng ta/tôi·quá, 6과 복습·mấy/bao nhiêu, 7과 빈도부사·sắp·hơi·nên·vì·hả, 8과 được·bao lâu·mới·đi/đừng, 9과 đã~chưa·bao giờ·xong·요일). jsdom: 부팅0·1~9과·홈버튼·단원전환(4/8/9과)·`_gramL` 저장·재진입시 마지막단원·screen키 grammar:N·회귀(홈·단어장·검색+병기·단어추가). WORDS 609·문장 819 유지.

- **2026-07-17 (Cowork — 하노이말 병기 + 문장 채점 버그 수정)**: 사용자가 "교재 문장이 호치민식이라 하노이식으로 바꿔줄 수 있냐"고 물어 조사 → **두 가지가 섞인 질문이었음**.
  - **① 어휘(남부말) — 병기함**: 단어장 609개를 훑어 **남부형만 실린 건 `trễ`(늦다)·`mắc`(비싸다) 딱 2개**. `NORTH` 맵으로 `하노이 muộn`·`하노이 đắt` 회색 배지를 **단어장·홈 검색**에 붙이고, **쓰기(한→베)에서 하노이말을 써도 정답** 인정(`checkVnAny`). **교재 뜻·문장은 안 건드림.** `muộn`/`đắt`으로 검색해도 교재 단어가 나옴.
    - **교재가 9·10과부턴 스스로 병기 중이었음** — `muộn/trễ` · `ba mẹ/bố mẹ` · `anh/anh trai` · `chị/chị gái` · `cảm thấy/thấy` 등 슬래시 표기. 그래서 `trễ`는 6과(단독)·9과(`muộn/trễ`) 둘 다 존재(중복 아님, 교재 그대로). ⚠️ NORTH 추가 전 **교재에 슬래시로 이미 있는지 확인**할 것.
  - **② 어순(사용자의 진짜 관심사) — 안 고침**: `không nói được tiếng Hàn`(하노이) ↔ `nói tiếng Hàn không được`(호치민). **교재가 반반 섞어 씀** — 남부 3곳(8과 122·123쪽, 대화1) / 북부 3곳(3과 34쪽, 8과 133·134쪽). **8과는 문법설명(134쪽)이 북부인데 회화(123쪽)는 남부.** 시험이 교재 기준이라 문장은 그대로 두고, **채점이 이미 양쪽 다 정답 처리**함을 확인(낱말 같고 순서만 다르면 ok) → 조치 불필요.
  - **③ `gradeSent` 채점 버그 수정**: `sentMatch`(편집거리 80%)로 '성조 차이'를 판정해서 **`mắc`→`rẻ`(뜻 정반대)도 "⚠️ 성조 차이"**로 나왔음. → **성조를 떼면 똑같을 때만 tone**, 아니면 no. (섹션7 참고)
  - jsdom: 부팅0 · 채점(mắc↔rẻ=no, đắt=no, 성조만 뺌=tone) · 어순 양방향 ok · 하노이말 정답인정/검색/병기 · 단어장·홈·시험지·쓰기설정 회귀. WORDS 609 · 문장 819 · 대화 26 유지.

- **2026-07-17 (Cowork — 단어 추가 자동완성 제거)**: 사용자 요청 — "괜히 자동완성 썼다가 그 자동완성 때문에 더 피곤해질 수 있을 것 같애". 바로 전 세션에서 자동완성 품질을 올리려 구글 번역을 붙였지만(`ghi nhớ`→`기억하기` 문제), **기능 자체를 없애는 쪽이 낫다고 판단**. 자동완성 코드 일체 삭제 → **베트남어·뜻 직접 기입**. 단어 추가에서 **인터넷 호출 0회**. (섹션5 참고) jsdom: 부팅0 · 자동완성 버튼/액션 없음 · blur·Enter에도 fetch 0 · 한국어 칸 자동 채움 없음 · 직접입력 추가/뜻 그대로 저장/영어 저장 · Enter 동작 · 삭제 · 홈검색·병기·단어장 회귀.

## (이전) 현재 상태 & 미해결 (2026-07-14 세션)

- **2026-07-14 (Cowork — 빠진 받아쓰기(Chính tả) 지문 3개 단원 보충)**: 사용자가 "듣기 문장 받아쓰기에 각 단원 dictation도 나오게?" 물어 확인 → **받아쓰기 지문은 원래 SENT_RAW에 문장 단위로 들어가 있어 이미 나오고 있었음**(4·5·8·9과). 다만 **2·3·7과는 빠져 있었음**(7과는 2026-07-10에 "이번엔 제외"라고 적어둔 것). → 사진에서 추출해 채움: **2과 29쪽 6문장 · 3과 47쪽 10 · 7과 119쪽 11**. **문장 총 792→819.**
  - ⚠️ **4과 받아쓰기 쪽번호 오류 수정**: 받아쓰기(65쪽) 지문이 `#59`(읽기 ĐỌC HIỂU) 마커 구간에 들어가 있어 정답지에 59쪽으로 나왔음 → `#65` 마커 삽입해 바로잡음.
  - **1·6과는 교재에 받아쓰기 섹션 없음**(6과는 복습 단원).
  - 받아쓰기 지문은 **별도 모드가 아니라 문장 풀에 섞여** 나옴 — 듣기 문장 받아쓰기·빈칸, 읽기, 순서배치, 시험지에 모두 포함.
  - jsdom 검증: 부팅0 · 문장 819/형식오류0/전 단원 중복0 · 쪽 100%(마커 샘 0) · 7개 단원 받아쓰기 지문 존재+쪽 정확(2:29·3:47·4:65·5:79·7:119·8:133·9:147) · 듣기 문장 받아쓰기에 2·3·7과 지문 포함 확인 · 문장빈칸·대화받아쓰기·시험지·홈 회귀.

## (이전) 현재 상태 & 미해결 (2026-07-14 세션)

- **2026-07-14 (Cowork — 9단원 문장 120 + 대화 3개 추가)**: 교재 Bài 9 «THỨ NĂM TÔI ĐI HÀ NỘI»(136~149쪽, 요일·날짜 / 문법 `đã...chưa` · `đấy` · `bao giờ` · `xong`) 사진 14장에서 추출.
  - **`SENT_RAW` 9단원 120문장** (쪽 마커 15개 포함) = 발음예문(136쪽) 9 · 회화 3편(137~138쪽) 22 · 읽기 ĐỌC HIỂU(142쪽) 8 · Đúng/Sai(143쪽) 5 · 받아쓰기 Chính tả(147쪽) 7 · 문법예문(148~149쪽) 27 · **쓰기 연습 원문·예시정답 42**(143~147쪽: `đã...chưa` 답하기 · `bao giờ` 의문문 · `đấy` 재작성 · `xong chưa`). **문장 총 672→792(L1~9).**
  - **`DIALOG_RAW` 9단원 대화 3개 / 22줄** (Eun Ji·Loan / Hiroki·Loan / Brian·Vân). `DIALOG_PAGES[9]=[137,137,138]`. **대화 총 23개·161줄 → 26개·183줄(L1~9).**
  - **9단원 단어 45개는 원래부터 RAW에 있었음** → 단어 안 건드림. WORDS 609 유지.
  - **쪽번호 100%** — 마커 15개로 136~149쪽 전부 덮음. 정답지에 `Lesson 9 회화1 · 137쪽` 식으로 표시됨.
  - ⚠️ **중복 1건 발견·수정**: `Sinh nhật chị là ngày nào?`가 **발음예문(136쪽)과 회화2(137쪽)에 둘 다** 있었음(교재에 실제로 두 번 나옴) → 발음예문 쪽을 지우고 회화 원문만 남김.
  - **제외**: 듣기 문항·선택지, 말하기 빈칸(정답 확정 불가).
  - jsdom 검증: 부팅0 · WORDS 609 유지(단어에 문장 안 섞임) · 문장 792/형식오류0/**전 단원 중복0** · 쪽 없는 문장 0(100%)/마커 샘 0 · 대화 26개 183줄/형식오류0 · 듣기 대화(9과 대화2 7줄·빈칸17, **9과 새 단어 13/17**) · 듣기 문장빈칸 · 순서배치 · 시험지(본지 출처 없음/정답지 10개) · 1과 대화·9과 단어쓰기·홈·검색 회귀.

## (이전) 현재 상태 & 미해결 (2026-07-14 세션)

- **2026-07-14 (Cowork — 1~7단원 쓰기 '의문문 전환' 재점검·수정)**: 8단원 작업 중 사용자가 "다른 단원에도 같은 사례가 있나?" 물어 **교재 쓰기 지시문을 전부 다시 읽음**. 지시문이 두 종류로 갈림 — ①**문법 지정형**(1과 `không`/`có...không`, 4과2 `đang`, 5·7과 `sẽ`·`quá`·`hơi`·`vì`·`hả`)은 정답 하나라 안전. ②**"A 또는(hoặc) B"형**은 8단원과 같은 구조라 정답이 여럿.
  - ⚠️ **3단원 43쪽 쓰기1이 명백한 오류였음**: 지시문은 `đâu, gì, ai, thế nào 또는 ...có...không, ...phải không`인데 **앱엔 `mấy` 의문문 8개**만 있었음(지시문에 없는 의문사 — `mấy`가 3과 문법 주제라 착각). → **8개 제거하고 9문항을 지시문대로 25개 재생성**(문항별 가능한 형태 다). 3과 56→73.
  - **2단원 26쪽 쓰기3**(`có...không` 또는 `phải không`, 교재 예시가 두 형태 다 제시) → `phải không` 형태 **6개 보강**. 2과 62→68.
  - **4단원 60쪽 쓰기1**(`có...không` 또는 `đâu, gì, mấy`) → 이 문제 답 **10개 추가**(기존 `bao nhiêu` 10개는 61쪽 다른 문제라 그대로). 4과 81→91.
  - **문장 총 624→672.** 단어 574 유지. jsdom: 부팅0 · 형식오류0 · 단원별 중복0 · 3과 mấy 의문문은 교재 회화·읽기 원문 3개만 남음(정상).

- **2026-07-14 (Cowork — 8단원 문장 161 + 대화 3개 추가)**: 교재 Bài 8 «ANH ẤY NÓI ĐƯỢC TIẾNG GÌ?»(122~135쪽, 외국어 능력 / 문법 `được` · `bao lâu` · `mới` · `đi` · `đừng`) 사진 14장에서 추출.
  - **`SENT_RAW` 8단원 161문장** = 발음예문(122쪽) 11 · 회화 3편(123~124쪽) 22 · 읽기 ĐỌC HIỂU(128쪽) 11 · Đúng/Sai 문장(129쪽) 5 · 받아쓰기 Chính tả(133쪽) 8 · 문법 예문(134~135쪽) 20 · **쓰기 연습 원문(진술문·상황문) 30** · **⚠️ 쓰기 변환 생성정답 54**(đi 제안문 8 · bao lâu 의문문 7 · mới 재작성 8 · **đi/đừng 제안문 16** · **의문문 전환 15**). **문장 총 478→639(L1~8).**
  - **정답이 하나가 아닌 두 문제도 사용자 선택으로 '가능한 형태 전부' 넣음**: ①쓰기2(đi/đừng 제안문, 130~131쪽 8문항) → **đi·đừng 두 형태 다 = 16**. ⚠️ `Các cô đến trễ đi!`(늦게 오세요!)처럼 **문법은 맞지만 뜻이 어색한 문장**이 섞임(사용자가 알고 선택). 6·7번은 힌트가 명사/형용사라 동사를 보탬(`đăng ký` · `đến`). ②쓰기1(의문문 전환, 129~130쪽 8문항) → **의문사 질문 + 예/아니오 질문 두 형태씩 = 15**(1번 `tiếng gì?`는 교재 예시 정답이라 이미 있어 제외). `thế nào?` · `có ~ không?` · `mấy thứ tiếng?` · `giỏi gì?` 패턴.
  - **`DIALOG_RAW` 8단원 대화 3개 / 22줄** (Vân·Loan / Toàn·Eun Ji / Emily·Dũng). **대화 총 20개·139줄 → 23개·161줄(L1~8).**
  - **8단원 단어 43개는 원래부터 RAW에 있었음**(교재 1~12과 단어는 앱 초기부터 포함) → 단어는 안 건드림. WORDS 574 그대로.
  - **제외**: 듣기 문항·선택지·빈칸 문제(정답 확정 불가). ⚠️ 생성정답 54개는 **교재 인쇄 정답이 아니라 규칙대로 만든 것** — 검수 대기.
  - ⚠️ **발견: 8단원 핵심 표현이 단어장(RAW)에 없음** — `được` · `đừng` · `mới` · `giùm` · `ngoại ngữ` · `thứ tiếng` · `nghe nói` · `hình như` · `thảo luận` · `khóc` 등. 교재 122쪽 PHÁT ÂM 1(단어·구 목록)에는 있는데 RAW[8]엔 빠져 있음. 그래서 **대화 빈칸의 '그 단원 새 단어 우선'(tier 0)에 이 핵심어들이 안 잡히고**, 홈 단어검색에도 안 나옴. **사용자 확인 후 RAW[8]에 추가할 것.**
  - ⚠️ **삽입 시 함정(다음에 조심)**: `SENT_RAW`에 단원 추가할 때 정규식 `(7:`...`)\n\};` 를 쓰면 **RAW(단어)의 `7:` 에 먼저 걸려** 문장이 단어 목록에 들어감. 반드시 `s.index("const SENT_RAW = {")` 로 블록을 먼저 잡고 그 안에서 처리할 것.
  - jsdom 검증: 부팅0 · WORDS 574 유지(8과 43개 그대로, 단어에 문장 안 섞임) · 문장 608/형식오류0/8과 중복0 · 대화 23개 161줄/형식오류0 · 듣기 대화(8과 대화3 6줄·빈칸13) · 듣기 문장빈칸(8과) · 순서배치(8과) · 읽기 문장(8과 130) · 시험지 8과 칩·생성 · 1과 대화·8과 단어쓰기·홈·영어검색 회귀.

## (이전) 현재 상태 & 미해결 (2026-07-12 세션)

- **2026-07-12 (Cowork — 듣기: 교재 '대화 빈칸 채우기' 신설)**: 사용자 요청("각 단원 dialogue를 빈칸 채우기 형식으로 — 들으면서 빈칸을 채워 넣게"). 게임화 논의는 사용자가 보류(경쟁 심리로 지치게 하고 싶지 않다 → 리더보드·스트릭 제외, 추후 SRS/타임어택/숙련도 후보).
  - **새 데이터 `DIALOG_RAW`** = `{단원:[대화1문자열, 대화2, …]}`, 대화 한 줄 = **`화자|베트남어|한국어`**. 교재 사진(uploads/N단원.zip)의 **HỘI THOẠI 페이지에서 화자 이름·대화 번호·줄 순서 그대로** 재추출 → `DIALOGS`(빌드) + `dialogLessons()`. **1~7과, 대화 20개 / 139줄** (1~5·7과 3개, 6과=복습단원이라 2개). ⚠️ **`SENT_RAW`(문장)와 별개 데이터** — SENT_RAW엔 회화 일부가 빠져 있었음(예: L1 "Chào anh David.", "V-Ơ-N.", "À..., xin lỗi."). DIALOG_RAW가 **온전한 회화**. 단원 추가 시 여기에 번호 키만 넣으면 화면·빈칸 자동 반영.
  - **화면**: 홈 → **듣기** → 설정에 **[대화] [문장]** 2택(사용자 요청으로 **듣기에서 '단어' 받아쓰기는 제거**, 대화가 맨 왼쪽·기본). `state.studyKind`에 **`dlg`** 추가(**듣기 전용**). `renderSetup` 진입 시 ①`dlg && !isListen` → `word`로 복구, ②`isListen && word` → `dlg`로 복구(기존 사용자의 저장값 정리). dlg면 **단원 칩 · 대화 번호 칩 · 난이도(쉬움/보통/어려움)** 카드가 뜨고 범위·문제수 카드는 숨김. `startDlg`→`renderDlg`. (`listenWordQ`는 죽은 코드로 남아 있음 — 되살리려면 `startSkill`의 listen 분기 복원.)
  - **빈칸 생성**(`dlgBuildLine`): 줄을 낱말/구분자로 토큰화(`dlgTokens`, 성조·대소문자 원문 보존) → ① **교재 단어(구 포함) 우선** (`dlgVocab`: **tier 0 = 그 단원 새 단어**, tier 1 = 앞 단원 단어. 같은 tier면 **긴 구 먼저** → `kế toán`·`ngân hàng`·`máy tính` 같은 핵심어가 뚫림. tier 없이 하면 `anh·là·không` 같은 기능어만 뚫려서 이렇게 고침) → ② 모자라면 일반 낱말(고유명사 `DLG_NAMES`·1글자 제외). **개수**(`dlgBlankCount`): 쉬움=줄당 1 / 보통=낱말의 25%(1~3) / 어려움=45%(최소 2), 항상 `낱말수-1` 이하. **`shuffle`이라 풀 때마다 자리가 달라짐**(사용자 선택: "핵심 단어 우선 + 난이도").
  - **듣기**: `speakLines(texts,onLine,onDone)` 신설(speak 옆) — 여러 줄을 **순서대로 큐잉 재생**, 지금 읽는 줄을 `.dline.now`로 하이라이트. 기존 끊김 방지(80ms 지연+`TTS_SEQ`+8초 keep-alive)·`TTS_FIX`(Eun Ji→Ưn Chi) 그대로 탐. `stopSpeak()`로 중단. 줄마다 🔊 개별 재생도 있음.
  - **힌트**: 한국어 뜻은 **기본 숨김**, 줄마다 `[힌트]` 버튼(`DLG.hint[li]` 토글)로 그 줄만 표시. 채점 후엔 전 줄 자동 표시. (사용자 선택)
  - **채점**(`dlgGrade`): 빈칸마다 **`checkVnTyped`로 성조까지** → `ok`(초록) / `tone`(⚠️ 노랑, 철자는 맞고 성조만 틀림) / `no`(빨강, 취소선 + 정답 초록 표시). 빈 칸=오답. 점수=ok 개수. 빈칸이 **교재 단어면 오답노트 반영**(맞으면 `noteRight`로 졸업 카운트, 틀리면 `noteWrong` — 단어 아닌 조각은 안 건드림).
  - **틀린 빈칸만 다시 풀기**(`dlgRetryWrong`, 2026-07-12 추가): 채점 후 버튼. `DLG.locked`(Set)에 **맞힌 빈칸을 잠가 채운 채 두고**(`.dblank done ok` span), **틀린 칸만 비워 input으로** 되살림. **정답(`.dans`)·힌트(뜻)는 다시 숨김** → 베껴 쓰지 못하고 소리를 다시 듣고 기억해서 써야 함. `DLG.retry`(회차) 증가 → 상단에 노란 안내 배너. 재채점 시 locked 칸은 자동 ok로 점수 합산. 다 맞히면 "다 맞혔어요" + 버튼 사라짐. `dlgRetry`(처음부터·빈칸 새로)는 `startDlg`라 `locked`/`retry` 초기화.
  - 채점 후 버튼: `틀린 빈칸 N개만 다시 풀기` / `처음부터 (빈칸 새로)` / `다음 대화 →`(단원 끝나면 다음 단원) / `그만두기`.
  - 상태 `DLG`, 액션 `dlgLesson/dlgNo/dlgLevel/dlgPlayAll/dlgStop/dlgHint/dlgGrade/dlgRetry/dlgNext/quitDlg`. LS 키 `_dlgL _dlgN _dlvl` 추가. CSS `.dline/.dwho/.dtext/input.dblank/.dblank.done.{ok,tone,no}/.dans/.dhint/.dplay`. `state.mode="dlg"`(→`startNote` 폴백 목록에 `dlg`,`bulk` 추가).
  - jsdom 검증: 부팅0 · 대화 20개/139줄/형식오류0 · 듣기에만 대화 옵션(읽기엔 없음) · 난이도별 빈칸 수(L3대화3 8/17/34) · **L4 대화1 빈칸 14개 중 9개가 4과 새 단어** · 채점 ok/tone/no 3색 · 오답노트 연동 · 힌트 토글 · 다시풀기 시 빈칸 자리 변경 · 전체듣기 6줄 순서 재생 + 이름 교정 적용 · 기존 단어/문장 받아쓰기·홈·시험지·단어장 회귀. ⚠️ 샌드박스에 브라우저가 없어 **실제 스샷은 못 찍음**(앱 CSS+렌더된 HTML로 미리보기만 확인) — 폰/맥에서 사용자 확인 필요.
  - ⚠️ **미해결**: 8과~ 대화는 교재 사진 받으면 `DIALOG_RAW`에 추가. 6·7과 문장·쓰기연습 생성문 성조 검수는 여전히 대기.

## (이전) 현재 상태 & 미해결 (2026-07-10 세션)

- **2026-07-11 (Cowork — 클라우드 동기화(선택적 로그인) + 4영역 색 되돌림)**: ①**4영역 카드 색 제거**(사용자: 시안이 "클로드 디자인 같다" → 학습앱엔 과함). `--sk-*` 변수·`.skillcard.sk-*` 규칙 전부 제거, **2×2 격자는 유지**하고 아이콘·글자로만 구분(아이콘/태그 `--muted`, 그림자 제거). 홈 검색바·배지는 유지. ②**클라우드 동기화 = 선택적 로그인**(사용자 결정): localStorage는 브라우저 안에만 있어 **브라우저 변경·데이터 삭제·시크릿모드에선 원리상 복구 불가** → 유일한 해법은 서버 저장 + 로그인. **Firebase(Auth 구글로그인 + Firestore)** 도입. 핵심: **`FIREBASE_CONFIG`가 `null`이면 로그인 UI가 아예 안 뜨고 앱은 기존과 100% 동일**(로컬·오프라인·로그인 없음) — 동기들 부담 없음. 로그인 시 `cloudSync`가 **클라우드→로컬 병합(union, 기존 것 안 지움) → 병합본 업로드**. 데이터 변경 시 `cloudSave`(1.2s 디바운스). SDK는 CDN에서 **동적 import + try/catch** → 오프라인/차단이면 조용히 로컬 모드. 저장 위치 `users/{uid}` (Firestore 규칙으로 본인 문서만 접근). 훅: `saveNote`/`addWord`/`delWord`. 부팅 시 `cloudInit()`. 설정 화면에 동기화 카드(로그인/로그아웃). **Firebase 프로젝트 생성 완료(사용자)**: 프로젝트 `wordnote` / `projectId: wordnote-db1dd` / 위치 `asia-southeast1(싱가포르, 변경 불가)` / Spark(무료) / Auth=Google 사용설정 / Firestore 규칙 = `match /users/{userId} { allow read, write: if request.auth != null && request.auth.uid == userId; }`(본인 문서만). `FIREBASE_CONFIG`에 config 입력 완료 → 기능 활성화됨. ⚠️ **승인된 도메인에 `chan026-normal.github.io` 추가 필요**(Authentication→Settings). ⚠️ **로그인 테스트는 https(GitHub Pages)에서만 가능** — `file://` 로컬 파일에선 구글 팝업 로그인 불가. Firebase 웹 config는 **공개돼도 안전**(보안은 Firestore 규칙이 담당). jsdom 검증(config 미설정 상태): 부팅0·로그인UI 미노출·단어추가/오답노트/졸업·홈/검색·시험지·문장 목록모드·단어장 전부 정상.

- **2026-07-11 (Cowork — 디자인 현대화 + 홈 단어검색 + Jane 발음)**: 사용자 요청(시안 승인 후 적용). ①**홈 맨 위에 단어 검색바**: `.hsearch`(알약형) + `#findResults`, `renderHome`에서 `bindFind()` 호출. **읽기 설정의 검색창은 제거**(중복 — 홈으로 이동). 빈 입력이면 결과칸을 아예 비워 홈이 깔끔하게. ②**디자인 현대화**: **4영역 카드 색 구분**(저채도, 쨍한 원색 금지 방침 유지) — `:root`에 `--sk-{read,listen,write,speak}` + `--sk-*soft`(라이트/다크 각각), `.skillcard.sk-*`는 배경 틴트 + 테두리·그림자 없음(납작한 모던 룩), 내부 `.nm/.ds/.tg`는 `color:inherit`(`.tg`는 currentColor 테두리). **오답노트 개수 배지**(`.badge` + `.btn.rowbtn`). ⚠️ **시험지(인쇄) CSS·`.paper`·`@media print`는 전혀 건드리지 않음** — `genPaper` 생성/정답지 검증 완료. ③**`TTS_FIX`에 `Jane → Chên`** 추가(베트남어 음성이 '자이'로 읽던 것). jsdom 검증: 부팅0·홈검색(결과 표시·빈입력 숨김)·4색 카드 클래스·읽기설정 검색제거·발음교정(vi만 적용)·시험지 생성·문장 목록모드/설정/단어장 회귀.

- **2026-07-11 (Cowork — TTS 끊김 수정 + 외국 이름 발음 교정)**: 사용자 지적 2건(맥북에어에서 발음이 매끄럽지 못하고 끊김 / 'Eun Ji' 발음 이상). ①**끊김 수정**: 기존 `speak()`가 `speechSynthesis.cancel()` **직후 곧바로** `speak()`를 호출 → **크롬(특히 맥) 알려진 버그**로 소리가 씹힘/끊김. **80ms 지연 + 시퀀스 토큰(`TTS_SEQ`)**으로 중복·겹침 재생 차단(연속 클릭 시 마지막 1건만), **긴 문장 중간 정지 버그**엔 재생 중 8초마다 `pause()/resume()` keep-alive(`TTS_KEEP`/`ttsKeepStop`, `onend`/`onerror`에서 해제). `pickedVoice` 자동선택을 **로컬(오프라인) 음성 우선**(`localService!==false`)으로 변경 — 온라인(network) 음성은 버퍼링으로 끊길 수 있음. ②**외국 이름 발음 교정**(`TTS_FIX`/`ttsText`): **화면 글자는 그대로 두고 '소리'만** 교정, **베트남어 재생일 때만** 적용(한국어 미적용). 베트남어 음성이 한국·영어 이름을 베트남어 규칙으로 읽어 이상하게 들리는 문제 완화. 현재 `Eun Ji → Ưn Chi`(은지) 1건. ⚠️ **샌드박스에 소리가 없어 귀로 검증 못 함 — 사용자 청취 후 튜닝 필요.** 이름 추가/수정은 `TTS_FIX` 배열에 줄만 추가하면 됨(Brian·Dorothy·Minami 등). jsdom 검증: 교정(vi만 적용)·로컬음성 우선·지연재생·연속호출 시 1회만 발화·한국어 미적용·속도 반영·회귀.

- **2026-07-11 (Cowork — 문장 '여러 문제 한 번에' 풀기 = 시험지 모드)**: 사용자 요청("문장문제를 리스트로 만들어 한꺼번에 풀게"). **쓰기 + 문장**일 때만 설정에 **'푸는 방식' 세그**(`state.sentBulk`, LS `_sbulk`): `한 문제씩`(기존) / `여러 문제 한 번에`. bulk면 `startSentQueue`가 **`startSentBulk`**로 분기. 새 상태 **`BULK`**={items,page,per:10,ans,res,graded,score} + 화면 `renderSentBulk`(10개씩 문항 카드: 번호·프롬프트·🔊·textarea) → **`bulkGrade`**(현재 페이지 일괄 채점, `gradeSent` 사용: ok=자동정답 / tone=⚠️'성조·철자 차이(시험 감점)' / no·빈칸=오답, 모범답안+🔊 표시) → 문항별 **`bulkToggle`**('맞은 걸로/틀린 걸로' 수동 정정, 점수·오답노트 동기화) → **`bulkNext`**(다음 10개, 마지막이면 `renderBulkDone`: 점수·정답률·다시 볼 문장 목록). 오답은 `noteWrong`(items에 `type:"sentence"`)로 오답노트에 저장. actions `sentBulk`/`bulkGrade`/`bulkNext`/`bulkToggle`/`quitBulk`. jsdom 검증: 토글이 쓰기+문장에만 노출·30문항/10개씩·정답=ok·성조뺌=tone(자동정답 아님)·엉뚱/빈칸=no·수동토글 점수반영·페이지 이동(11번부터)·결과화면·'한 문제씩' 회귀·홈 정상. LS 키 `_sbulk` 추가.

- **2026-07-11 (Cowork — 홈 정리: 버튼 9→6, ⚙️설정 화면 신설)**: 사용자 "앱이 점점 복잡해진다" → 정리. ①**'문장 시험' 삭제** (`goSent`/`renderSentSetup`/`startSent` + actions `goSent`/`startSent`/`sentLesson`/`sentDir` 제거) — **'쓰기 → 문장'(`startSentQueue`)과 완전 중복**이라 없앰. ⚠️ `state.sentLesson`은 `startSentQueue`/`renderSetup`에서 계속 쓰므로 **유지**. ②**⚙️ 설정 화면 신설**(`goSettings`/`renderSettings`, `#i-gear` 심볼 추가) — **발음(소리) 설정 · 베트남어 타자(Telex) · 백업/복원**을 그 안으로 모으고 홈에선 제거. ③**'단어 추가'를 단어장 안으로**(`renderList` 컨트롤에 chip) — 홈에서 제거. **결과 홈 = 4영역 카드 + [오답노트] [실전/플래시] [시험지] [단어장/설정]** (버튼 9개+링크 → 6개). jsdom 검증: 부팅0·홈 구성·제거 확인·설정 진입·단어장 추가버튼·문장/단어 모드 회귀. ⚠️ **작업 중 맥에서 git 작업(커밋/체크아웃)이 돌아 working tree가 되돌려져 일부 수정이 날아갔고, actions엔 `goSettings`만 남아 앱이 잠시 깨졌음 → 재적용해 복구. Cowork 편집 중엔 맥에서 working tree 바꾸는 git 명령(checkout/reset/stash) 금지.**

- **⚠️ 2026-07-10 (Cowork — 읽기 지문 문장 잘림 발견·수정)**: 사용자가 교재 **78쪽 읽기(5단원 ĐỌC HIỂU)** 문장이 앱에서 잘려 나온다고 지적 → 사실이었음. 원문 `Người thích đi dạo, nói chuyện với bạn bè, **đi với gia đình** thường đến Phố đi bộ Nguyễn Huệ **vào buổi chiều, buổi tối cuối tuần**.` 인데 앱엔 `...với bạn bè thường đến Phố đi bộ Nguyễn Huệ.` 로 **뒷부분이 통째로 누락**. 다음 문장도 앞의 `Còn` 누락. 둘 다 수정(L5 63문장 유지, 전체 478). **원인**: 교재 지문이 여러 줄로 **줄바꿈되어 인쇄**돼 있어 사진에서 추출할 때 문장 끝을 잘못 잡음. ⚠️ **같은 유형(줄바꿈 긴 문장)의 잘림이 다른 단원 읽기 지문·긴 회화 대사에도 있을 수 있음** → 시간 나면 1~7단원 ĐỌC HIỂU 지문을 사진과 1:1 대조 검수 권장.

- **2026-07-10 (Cowork — 데이터 지킴이: 영구저장 요청 + 백업 넛지 + 안내)**: 사용자 질문("업데이트하면 남의 단어장 초기화?" → **아니오**: localStorage는 기기별·origin별이라 코드 push로 안 지워짐. 단 KEY/도메인/`_custom`·`_notebook` 구조 변경·`localStorage.clear()`는 금지. iOS 사파리 7일 삭제·기기별·수동삭제가 진짜 원인). 대비책 3종 추가(모두 lsSet 아래 헬퍼): ①**영구저장 요청** `requestPersist()`(부팅 시 `navigator.storage.persist()`, 결과 `PERSISTED` 캐시) — 안드로이드/크롬 효과, iOS 사파리는 미보장. ②**백업 넛지** — `bkCount()`(내단어+오답 수)−`_lastbk`(마지막 백업 카운트)≥5면 홈에 부드러운 배너(`bkNudgeHTML`, warn색 카드, skillgrid 아래): '나중에'(`snoozeBackup`=현재값으로 리셋)/'백업하기'(`goBackup`). 백업·복원 시 `bkMark()`로 리셋(`copyExport`·`downloadExport`·`importBackup`에 hook). ③**백업 화면 데이터안전 안내**(`renderBackup` 상단 brandsoft 카드): 백업 제일 확실 / 아이폰 홈화면추가+가끔열기 / 영구저장 자동요청(+`PERSISTED` 상태 표시). jsdom 검증: persist 호출·넛지(0없음→6배너→스누즈숨김→+5재등장→백업리셋)·백업화면 안내·회귀. LS 키 `_lastbk` 추가.

- **2026-07-10 (Cowork — 문장 채점에 성조 반영, 사용자 지적)**: 위 '똑똑한 자동 채점'이 성조를 무시(`sentMatch`가 성조 제거 비교)해 **성조 오류를 '정답'처리** → 실제 시험 감점과 불일치한다는 지적. **3단계 `gradeSent`(ok/tone/no)** 추가: ok=성조·철자 정확(어순만 다른 건 허용 — `normSentTone`으로 성조 유지 비교 + 정답 토큰 전부 포함) / tone=성조 빼면 맞음(글자OK·성조/dấu만 다름) / no=내용도 다름. `checkSent`가 `gradeSent`로 채점 — **ok만 자동 정답**, tone이면 `Q.toneWarn`으로 '⚠️ 성조·철자 달라요, 시험 감점 가능' 배너 + 모범답안 보여주고 자가채점(자동 정답 아님), no는 기존 자가채점. 단어 쓰기(en2vn)는 원래 `checkVnTyped` 성조 채점이라 그대로. jsdom 검증: gradeSent(완전일치·어순바뀜=ok / 성조없음·1개틀림=tone / 엉뚱=no), 퀴즈 흐름(완벽→autoOk·score+1, 성조뺌→toneWarn·자동정답아님·배너, 엉뚱→자가채점), 단어 회귀. `Q.toneWarn`은 `setupQ`에서 리셋.

- **2026-07-10 (Cowork — 4영역 문장 공부 + 읽기 단어찾기 + 문장 스마트 자동채점)**: 사용자 3건. ①**읽기·쓰기·듣기·말하기 모두 '단어/문장' 선택**: `state.listenKind`→**`state.studyKind`**(word|sent, LS `_skind`)로 일반화. `renderSetup`에 4영역 공통 토글(듣기만 "무엇을 받아쓸까요?"), 문장이면 문장단원 칩(`studySentLesson`). `startSkill`이 `useSent`면 **`startSentQueue(skill)`**로 분기: 읽기=mode `read`·`{type:sentence,dir:vn2en,prompt:vn,answer:뜻}`(읽고 '뜻 보기'), 쓰기=mode `type`·`makeQ(...,"sentence")`(번역+채점), 듣기=`listenSentQ`(받아쓰기), 말하기=`startSpeak(문장,true)`(`SP.cards[].sent`→`.prompt.sent`). 기존 '문장 시험' 버튼(`goSent`/`startSent`)은 그대로 둠(중복이나 무해). ②**읽기 화면 '단어 찾기'**: read 설정에 검색창(`bindFind`)—입력하면 `WORDS`에서 `norm` 부분일치로 'Lesson N M번·베트남어·뜻·🔊' 최대 40개(화면 재생성 없이 결과만 갱신). ③**문장 자동 채점 강화(오프라인, 사용자 A안)**: `sentMatch`에 **토큰집합 겹침**(`sentTokens`) 추가—편집거리 유사도≥0.8 또는 모범답안 핵심어의 85%가 내 답에 있으면(어순·불필요어 무시) 자동 정답. 못 잡으면 기존대로 모범답안 보고 자가채점(**자동 오답처리는 절대 안 함**). '완벽한 의미 판정은 오프라인 불가(LLM 필요)'라고 사용자에게 설명·동의받음. jsdom 검증: 부팅0·4영역 토글·exam엔 없음·검색결과·read/write/listen/speak 문장큐·sentMatch(어순/성조/엉뚱)·단어모드 회귀·기존 문장시험 유지. ⚠️ 한국어 답(vn2en 번역)의 다른 표현은 조사·활용 탓에 토큰이 안 맞아 여전히 자가채점 남을 수 있음(오프라인 한계).

- **2026-07-10 (Cowork — 발음 음성 선택 + 속도 조절)**: 사용자 질문("발음 기반이 뭔지, 하노이/호치민?, có thể의 có가 안 올라감")→설명 후 개선 요청. 앱은 자체 발음 없이 **기기 내장 TTS(vi-VN)** 재생일 뿐(`speak`에 특정 음성·속도 지정 없던 걸 보강). **음성 선택 + 속도 조절** 추가: `speak`가 `pickedVoice`(vi 음성 중 사용자 선택 `_voiceURI`, 없으면 enhanced 우선)+`ttsRate`(`_rate` 0.6~1.3, 기본 0.9) 반영, 베트남어일 때만 음성 적용. 새 화면 `renderVoice`/`goVoice`(홈 '발음(소리) 설정'): vi 음성 목록 칩·속도 슬라이더·"테스트 듣기"·기기별 향상된 음성 설치 안내. 액션 `pickVoice`/`testVoice`, `loadVoices`+`onvoiceschanged`. jsdom(음성 모킹) 검증: viVoices 필터·enhanced 자동우선·speak가 voice/rate 반영·한국어 미적용·pick 저장/복귀·화면/홈버튼·기존 회귀0. (섹션5 발음 항목 참고.) 'có 안 올라감'은 성조는 맞고 기본음성 성조폭·rate 0.9·연접 탓 → 향상된 음성/속도 1.0로 완화 가능.

- **2026-07-10 (Cowork — 듣기를 '베트남어 받아쓰기'로 개편 + 문장 받아쓰기)**: 사용자 요청. 기존 듣기(베트남어 듣고 **한국어 뜻** 입력)를 **소리만 듣고 들은 베트남어를 그대로 쓰기**로 바꿈. 듣기 설정에 **단어/문장 토글**(`state.listenKind`, 문장은 문장단원 칩), **방향 선택 제거**(받아쓰기 고정). 문제=`{dir:'listen',dictation:true,prompt=answer=vn,meaning=en}`(`listenWordQ`/`listenSentQ`/`startListenSent`). 단어채점 `checkVnTyped`(성조까지, 그 단어만; 성조만 틀리면 "철자는 맞음" 안내), 문장채점 `sentMatch` 관대+자가채점. 정답공개 시 **뜻(한국어)** 표시. 오답재시도 `rebuildQ`로 받아쓰기 복원. jsdom 검증: 부팅 무오류·단어/문장 큐(dictation·prompt=answer=vn)·성조 toneOnly·기존 쓰기/읽기 방향 유지·홈 부팅. (섹션5 듣기 항목도 갱신.) ⚠️ 샌드박스 크롬 없어 스샷 미실행 → 실제 화면은 사용자 확인. push는 맥 Claude Code.

- **2026-07-10 (Cowork — 7단원 문장 46개 + 카톡 카드 제목 개선)**: ①**7단원 문장 추가**: 교재 Bài 7 «TÔI THƯỜNG KHÔNG ĂN SÁNG»(107~121쪽, 일상생활·아침식사·빈도부사) 사진 15장에서 회화3(Dorothy/Vân·Eun Ji/Dũng·Loan/Lee, 108~109쪽)+읽기(114쪽 ĐỌC HIỂU)+문법예문(120~121쪽 sắp vs sẽ·hơi·hả·nên) 추출 → `SENT_RAW` 7단원 **75문장** = 회화3·읽기·문법예문 **46** + 쓰기연습(THỰC HÀNH VIẾT) **변환정답 29**(hơi 재작성 10·vì 전환 9·hả 의문 10, 사용자 "1~6단원처럼" 요청). **문장 총 403→478(L1~7).** 발음섹션(107쪽)은 회화와 중복이라 제외, sắp·nên **완성형(빈칸 자유응답)은 정답 확정 불가라 제외**, 받아쓰기(Chính tả, 119쪽) 지문도 이번엔 제외(요청 시 추가 가능). ⚠️ 변환정답 29개는 **교재 인쇄답이 아니라 규칙대로 생성**(1~6단원 방식) — 성조·표현 검수 대기. jsdom 부팅 무오류·형식오류0·L7=75·allSentences 478·신규중복0 검증. (이미지: outputs/dan7_fixed, contact/dan7_contact.jpg — EXIF 보정.) ②**카톡 미리보기 제목 어폐 수정**(사용자 지적 — '단어 시험'인데 실제론 문장·읽듣쓰말 4영역 다 포함): `<title>`+OG 태그를 **'베트남어 단어·문장 학습 · 베트남어 ↔ 한국어'**로(index.html·tiengviet.html **둘 다**), `meta description`/`og:description`/`og:url`(Pages 루트) 추가 → 카톡 기본문구 "여기를 눌러 링크를 확인하세요" 대체. 앱 상단 홈버튼도 '단어 시험'→**'베트남어 학습'**(약 303행). **⚠️ 카톡은 미리보기 캐시 → 공유 시 `?v=2` 붙이면 즉시 갱신.** 백업 파일명(단어시험_백업.json)·기타 라벨 유지. (Cowork 편집, push는 맥 Claude Code에서.)

## (이전) 현재 상태 & 미해결 (2026-07-09 세션)
- **2026-07-09 (시험지 문장범위 다중선택 + 스크롤 유지)**: 사용자 버그제보 2건 수정. ①**문장 범위 다중선택**: `P.sentLesson`(단일값 "all"|번호)→**`P.sentPick`(Set, 빈 Set=전체)**. 칩 토글(추가/해제), '전체'=비움. `genPaper`의 `sentPool`=선택 단원들 `paperSents` 합(빈 Set이면 `allPaperSentences`). 칩 on 상태 `P.sentPick.has(l)`/전체 `size===0`. 액션 `paperSentLesson` 토글로 변경. ②**시험지 컨트롤 클릭 시 맨위로 튀는 것 수정**: `set()`이 매 렌더 `scrollTo(0,0)` 하던 걸, 헬퍼 **`paperKeep(fn)`**(스크롤 y 저장→fn→복원)로 감싸 시험지 컨트롤(`paperDir`/`paperCount`/`paperFormat`/`paperPriority`/`paperOne`/`paperSentLesson`) 재생성 때 위치 유지(첫 진입 `goPaper`는 그대로 맨위). 헤드리스 검증: 3과+5과 동시선택 pool=119, 토글/전체 정상, 다중선택 칩 하이라이트 확인.

- **2026-07-06 (Cowork — 쓰기 연습 '정답 문장' SENT_RAW 추가, 데이터만)**: 각 단원 쓰기(THỰC HÀNH VIẾT) 변환문제(문장→의문문/부정문 등)의 빈칸 정답을 **문법 규칙대로 생성**해 `SENT_RAW`에 추가(코드/함수 변경 없음). 1과 +18(có…không 의문 9+부정 9)·2과 +16(ai 의문 10+부정 7)·3과 +15(mấy 의문 8+đã 과거 7)·4과 +10(bao nhiêu 의문 10)·5과 +14(sẽ 미래 10+quá 강조 4)·6과 +22(có·phải không/ai·gì·đâu·nào/mấy·bao nhiêu 의문). **문장 총 308→403.** 제외: 철자 고르기(ten/tên)·cũng 완성·빈칸 대화(회화 중복)·받아쓰기 지문. **⚠️ 교재 인쇄 정답이 아니라 규칙대로 생성한 변환문이라 성조·표현 오류 가능 — 나중에 검수 필요.** 헤드리스로 단원별 수(L1 53·L2 62·L3 56·L4 81·L5 63·L6 88=403)·형식오류0·부팅 검증.
- **2026-07-06 (Cowork 작업 — 읽기/쓰기 개편·동의어 확대·6단원)**: ①**쓰기에 방향 선택 추가**: `renderSetup`의 `showDir`에서 write 제외를 풀어 쓰기도 한→베·베→한·랜덤 선택 가능(읽기는 vn2en 고정 유지). `startSkill`을 `makeQ(w, dirOf(state.dir))`로 바꿔 '랜덤'이 문항마다 실제로 섞이게. `SKILLS.write` tag "한 ↔ 베"·desc 갱신. ②**읽기를 '입력 없이 읽고 뜻 확인'으로 개편**: `SKILLS.read.mode` "type"→"read". `renderQuiz`에 `kind==="read"` 분기(입력칸 대신 '뜻 보기'=`revealRead`로 한국어뜻+발음 공개, 채점·점수 없음, ☆'다시 볼 단어'=`readStar`로 오답노트 추가). 완료화면 `renderReadDone`, `renderResult`가 read면 그리로. `startNote` 모드 폴백에 read 추가. ③**쓰기 동의어 정답 확대**: `checkVnAny`를 넓혀, 뜻 표기가 완전 일치 안 해도 **뜻 조각(콤마/슬래시)이 겹치는 실제 교재 단어의 베트남어**면 정답(`meaningTokens`/`meaningsOverlap`). 예: 뜻 "얼마, 몇"에 `mấy`(몇) 입력→정답(성조채점·과인정 방지 유지). ④**6단원 문장 66개 추가**: `SENT_RAW` 6단원(교재 복습 87~106쪽 회화2·읽기·작문·문법예문, 한국어 직접번역). **문장 총 242→308(L1~6).** (사진 추출이라 성조/번역 오류 가능 — 회전 페이지 재확인 권장.) 헤드리스 검증: 부팅·읽기모드 화면·쓰기 방향선택·동의어확대·SENT6=66 확인.
- **2026-07-04 (Cowork 미커밋분 — 이번에 함께 올림)**: ⑤**단어장 '여러 단원 모아 저장'**: 단어장에서 여러 단원을 골라 한 Word(.doc)로 저장(단원 열 포함). `L.multi`/`L.pick`(Set), `pickedWordsAll`/`pickedLabel`/`lessonTag`/`downloadDoc`/`docFileName`/`exportPicked`, `wordDocHtml`에 `withLesson` 옵션, `renderList` 다중선택 분기, actions `toggleMulti`/`pickLesson`/`pickAll`/`pickClear`/`exportPicked`.

## (이전) 현재 상태 & 미해결 (2026-07-04 세션)
- **2026-07-04 (5단원 문장 49개 + 발음예문 시험지 재포함)**: ①`5단원.zip`(제68~86과 "BÂY GIỜ CHÚNG TA ĐI ĐÂU?/어디 가요", 주소·장소·큰숫자 nghìn/triệu/tỷ·chúng ta·sẽ·quá·à·đấy) 사진 19장에서 회화3(Vân/David, 택시/Dorothy, Lee/Toàn)+읽기(보행자거리 Nguyễn Huệ/Bùi Viện)+받아쓰기+문법예문(chúng ta·đấy·quá·à·sẽ) 추출 → `SENT_RAW` 5단원 **49문장** 추가. 문장 총 193→**242**. (5단원은 발음섹션에 '각 câu' 없어 lr 대상 없음.) ②**'발음 예문(lr) 시험지 제외' 되돌림**(사용자 요청): `paperSents = l => (SENT_BY_LESSON[l]||[])`로 필터 제거 → 시험지도 모든 문장 포함(allPaperSentences=allSentences=242). `@`/lr 표시는 데이터에 남겨둠(다시 제외하려면 `.filter(s=>!s.lr)` 추가). 사진 정방향(회전 없음).
- **2026-07-04 (수업 노트 단어 42개 추가)**: 사용자가 준 `베트남어_수업_정리노트.docx`(7/2 수업, 직업·장소·숫자·시간·형용사·일상활동·언어실력)에서 어휘 85개 추출 → 기존 RAW와 중복검사(43개 기존, 42개 신규) → **신규 42개를 `RAW` 14단원으로 추가**. 여러 주제라 특정 교재단원에 못 넣어(사용자 의견) **번호 대신 라벨 `LESSON_NAMES[14]="7/2 복습"`**(칩에 '7/2 복습'으로 뜸, '오늘 배운 ★'과 별개). WORDS 532→**574**. 영어 뜻(ENG_MAP) 없어 시험지 en2vn은 한국어 폴백. docx 파싱은 zip+document.xml. (중복 43개는 기존 뜻 유지—일부는 노트 뜻이 더 정확: gần=가깝다 vs 기존 거의/가까이, lái xe=운전기사 vs 운전하다 — 필요시 갱신.)
- **2026-07-04 (읽기/쓰기 방향 고정 + 문장 시험 타자화)**: ①**읽기·쓰기 방향 고정**(사용자: "읽기·쓰기가 같아 보임" → 구분). `read`=`vn2en`(베→한, 읽고 뜻 맞히기)·`write`=`en2vn`(한→베, 뜻 보고 베트남어 쓰기)로 **방향 선택 버튼 제거**(`renderSetup`의 `showDir`에서 read/write 제외 — 듣기·말하기는 방향 선택 유지). SKILLS 태그 `베↔한`→`베→한`/`한→베`, 설명·cardDesc 갱신. **⚠️ 예전 '읽기·쓰기 둘 다 양방향' 방침을 뒤집음**(이번 사용자 요청). ②**문장 시험 타자 입력**(사용자: "직접 타자로", "딱 안 맞아도 맞는 말이면 정답"). 문장 화면에 `<textarea id="sentInput">` + '채점하기'/'모르겠어요'. `checkSent`→`sentMatch`로 **관대한 자동채점**: `normSent`(소문자+**성조/diacritic 제거**(NFD+combining 제거+đ→d)+문장부호·띄어쓰기 무시) 후 완전일치 또는 편집거리 유사도≥0.8이면 자동 정답. 못 맞추면 모범답안+'내 답' 보여주고 **자가채점**(맞음/틀림 — 표현 달라도 맞으면 '맞았어요'). Enter=채점, Shift+Enter=줄바꿈. `Q.autoOk` 플래그. (오프라인이라 완전 의미판정 불가 → 관대매칭+자가채점 하이브리드.) 실전시험 문장 10개에도 동일 적용. 헤드리스 검증: 정답/성조없이=자동정답, 엉뚱=자가채점, 방향선택 read/write 없음·listen 유지.

## (이전) 현재 상태 & 미해결 (2026-07-03 세션)
- **2026-07-03 (시험지 '뜻→베' 영어 + 단원 전체해제 시작)**: ①**시험지 '뜻→베트남어'(en2vn) 문제의 뜻을 교재 영어로 출제**(사용자 요청, '베→뜻'(vn2en)·화면학습·플래시카드는 한국어 유지). 원본 영어판(커밋 `a3f3653`, 한국어화 전)에서 영어 뜻 복원 → **`ENG_RAW`(단원별 vn|english, RAW 뒤)** + **`ENG_MAP`**(키 `단원||norm(vn)` — 동형이의 정확 매칭: 예 anh=you vs Anh=England, thứ hai=monday/second, mất=to take/to die). `buildWords`가 `w.eng` 부여, `genPaper`에서 en2vn 프롬프트=`w.eng||w.en`(영어 없으면 한국어 폴백). **교재 1~12과 496개 영어 있음, 13과 30개·custom은 영어 없어 한국어 폴백.** 정답지도 같은 qs라 영어 프롬프트로 나옴. ②**앱 시작 시 단원 '전체 해제' 상태**(시험지·홈 모두, 예전 시작부 전체선택 1줄 제거) — 필요한 단원만 골라 씀. 헤드리스 검증: en2vn 290개 프롬프트 100% 영어(29번 'England'=Anh 확인)·vn2en 정답 100% 한국어·startup state.lessons=0.
- **2026-07-03 (시험지에서 '발음 예문' 제외)**: 교재 'Nghe và lặp lại các câu'(Listen and repeat, 발음 연습용 예문) 문장을 **시험지(genPaper)에서만 제외**(사용자 요청 — 아직 안 배운 단어가 섞이고 발음 위주라 시험 가치 낮음). **문장 시험(goSent 자습)에는 그대로 유지.** 구현: `SENT_RAW` 해당 줄 앞에 **`@` 접두사** → 파서가 `lr:true` 플래그 부여. 헬퍼 `paperSents(l)`=lr 제외, `allPaperSentences()`. genPaper `sentPool`·시험지 '문장 범위' 칩 개수는 이 헬퍼로 교체(`allSentences`/`SENT_BY_LESSON`은 goSent용 그대로). **대상: 1과 0 · 2과 0 · 3과 3(34쪽 초록박스: Các ông ấy muốn.../Có mười người đến muộn./Xin lỗi buổi tối...) · 4과 14(51쪽 ②섹션 6 + 52쪽 ④섹션 8).** 1·2과 발음예문은 애초에 SENT_RAW에 없음(2과 소개예문은 문법섹션 출처라 유지). 문장 총계: goSent 193 / 시험지 176(전체), 3과 41→38·4과 71→57. Chrome 헤드리스로 40회 생성 시 lr 0회·goSent L4 71문장 유지 검증. (교재 사진: `~/Downloads/N단원.zip`, 3·4단원은 회전 없음, 1·2단원도 upright.)
- **2026-07-03 (4단원 문장 추가)**: 교재 4단원(제50~67과 "ANH LÀM NGHỀ GÌ?/직업") 사진 18장에서 핵심 회화·예문·읽기 추출 → `SENT_RAW`에 `4:` 71문장 추가(회화 3개, 예문 섹션2·4, 읽기 ĐỌC HIỂU, 받아쓰기 Chính tả, 문법 예문 đang·rất·lắm·nào·thế nào·bao nhiêu). 발음연습(c/k/qu/g/gh/ng/ngh)·학생 손글씨 빈칸문제는 제외. 단어(WORDS)는 4과 44개가 이미 있어 문장만 추가. Chrome 헤드리스로 문장시험 화면(Lesson 1~4)·1번문항·부팅 검증. **문장 총 122→193개.** (⚠️ 검증 팁: 헤드리스 주입 시 `</body>`로 replace하면 `wordDocHtml` 템플릿 안의 `</body>`에 걸려 스크립트가 깨져 보임 — `rfind`로 마지막 `</body>` 앞이나 `</script>` 기준 주입할 것.)
- **2026-07-03 (Cowork 작업)**: ⑤**단어장 Word(.doc) 저장** — 지금 보이는 목록(단원/검색/내 단어)을 Word 문서로 내려받기(`wordDocHtml`/`exportWordDoc`, '단어장 보기'에 'Word로 저장' 버튼, `exportWord` 액션). 화면·저장 공용 필터로 `curListWords`/`curListLabel` 추출해 `renderList` 정리. ⑥**쓰기(한→베) 동의어 정답 인정** — 같은 한국어 뜻을 가진 다른 베트남어도 정답 처리(성조 채점 유지). `checkVnAny` 추가, `checkTyped`의 `en2vn` 분기를 `checkVnTyped`→`checkVnAny`로 교체(예: 뜻 "매우, 아주" → `rất`·`lắm` 모두 정답, 헤드리스로 확인). ⑦**화면 확대** — 헤더에 −/100%/+ 버튼, `#view`에 `zoom` 적용(0.8~2.0, LS `_zoom`), 직접 리스너 `bindZoom`, 인쇄 시 리셋(`@media print .wrap{zoom:1!important}`), 좁은 화면(≤430px)에선 `.sub` 숨김. (JS 문법·부팅·기능 헤드리스 검증 후 로컬 커밋/push.)
- **2026-07-01 (Cowork 작업)**: ①단어장 '내 단어' 칩을 '전체' 뒤로+개수, 전체보기에서 내 단어 맨 위 고정·accent 구분(`.chip.mine`/`.lz.mine`, `--accentsoft`/`--warn`), '내 단어'만 볼 때 추가·관리 힌트. ②**단어 추가 자동완성**: 한쪽만 입력→나머지 채움. 교재(`WORDS` 532)에 있으면 오프라인 즉시, 없으면 MyMemory 무료 API(vi↔ko) 번역+'확인 필요' 강조(`lookupLocal`/`translateOnline`/`doAutoFill`, `autoFill` 액션, blur/Enter/버튼 트리거). ③추가 후 '추가됐어요' 메시지 자동 페이드(`setAddStatus` autoClear+`.addstatus`). ④쓰기(주관식) Enter 버그: 입력창 Enter `preventDefault`/`stopPropagation` → 첫 Enter는 채점만, 다음은 두번째 Enter/'다음'. (Cowork에서 편집, push는 로컬에서)
- **2026-06-27 추가 세션 ④**: 시험지 문구 다듬기 — ①"문장 번역"→"문장" ②범위 표기 `L1,L2`→`1과,2과`(`rangeTitle` `l+"과"`, 13단원은 라벨) ③제목 "베트남어 시험 (단어+문장)"→"베트남어 시험" ④**정답지를 시험지와 동일 밑줄 레이아웃**으로 개편(정답을 줄에 초록 채움). 2장 유지(Chrome PDF 검증).
- **2026-06-27 추가 세션 ③**: 시험지 단어 번호 **세로(열 방향) 정렬**(왼1~N→오른N+1~, `grid-auto-flow:column`+`--prows`). 시험지·정답지 모두. Chrome 헤드리스 PDF로 1~20/21~40 확인.
- **2026-06-27 추가 세션 ②**: **UI 이모지 → 인라인 SVG 라인 아이콘 전면 교체**(87+곳, `<symbol>` 스프라이트 + `.ic`). 화살표·국기·☆★는 문자 유지(섹션7). Chrome 헤드리스 `--screenshot`으로 홈·설정·시험지·단어장·오답노트·말하기·퀴즈 7화면 검증. 검색창 placeholder는 속성이라 아이콘 빼고 텍스트로.
- **2026-06-27 추가 세션**: 시험지 **정답지**(문제 순서대로, 정답 초록) + **A4 한 장에 맞춤**(`P.onePage` 압축, **기본 ON** → 시험지1장·정답지1장) + **시험지/정답지 따로 저장**(`printOnly`, 인쇄 버튼 2개 — `P.withKey` 토글은 이걸로 대체·제거) + **시험지 문장 "전체"=단원 합 122개 버그수정**(`allSentences`). 모두 Chrome 헤드리스 print-to-pdf로 페이지수·내용 실측 검증(섹션3 참고).
- **이전 세션 큰 작업(커밋순)**: 4영역 학습(`ccf0e37`) → 오답노트 시드(6/22~25 시험 오답) → 숫자채점·오답방향 → 시험지 우선출제+13단원 단어 → 단원별 문장시험(`SENT_RAW`)+시험지 문장범위 → 오답 직접입력 → 성조채점+양방향+**SEED 비활성**(`32610fa`) → 6/25 학습단어 → 전체해제 버그수정+양방향표시 → 1·3단원 문장 → 백업버튼 축소 → 시험지 빈범위 처리.
- **⚠️ 동기 공유 워크플로(중요)**: 사용자가 앱을 GYBM 동기들과 Pages URL로 공유 중. 그래서 `seedNote()`를 껐음(새 사용자=동기는 빈 오답노트). **그 결과, 사용자 본인의 시험 오답을 코드(`SEED_NOTE`)에 넣으면 동기에게도 떠서 안 됨.** → 사용자 오답은 **백업 JSON으로 만들어 주고** 사용자가 폰 백업/복원으로 가져오거나(예: 6/27 오답은 JSON으로 제공함), **'➕ 오답 직접 추가'**로 입력. **코드에 넣는 건 '모두에게 공유되는 학습자료'(13단원 단어·`SENT_RAW` 문장)만.** ← 이 구분 꼭 지킬 것.
- **⚠️ 미검증**: 마지막 커밋(시험지 빈범위 처리)은 **preview 서버가 권한 오류(`PermissionError`)로 안 떠 화면 검증 못 함**. 사용자 폰 확인 대기. (평소엔 preview 잘 됐는데 세션 끝에 막힘 — 새 세션에서 재시도해볼 것.)
- **문장/단어 정확도**: 교재 사진(카톡, 90° 회전 → `sips -r 90` 보정 후 읽음)에서 베트남어 본문 추출, 한국어는 직접 번역. **성조 일부 오류 가능** — 사용자가 발견 시 수정. 각 단원 사진 010~ 일부(받아쓰기·반복연습)는 미추출(핵심 회화·예문·읽기 위주).
- **다음 예상**: 8단원~ 교재 사진 주면 `SENT_RAW`에 추가(7단원까지 완료). 6·7단원·쓰기연습 생성문장(hơi·vì·hả 등)은 **검수 대기**(성조·번역 확인). 디자인/효과음/통계 등은 사용자가 '나중에'로 미뤄둠. 사용자가 매 시험 오답을 줄 때마다 위 '동기 공유 워크플로'대로 처리.
